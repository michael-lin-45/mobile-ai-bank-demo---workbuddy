# 2026-07-11 工作记录

## R32 — AI 上层应用可观测目标架构方案（综合交付）
- 用户聚焦 Agent+LLM 的**业务指标 / Trace / 会话**三类数据，并追加 **AI 洞察层**（运营/运维提炼：性能瓶颈、准确率提升点、业务转化瓶颈）。
- 交付物：
  1. 内联 SVG 目标架构图（`ai_agent_observability_target_arch`）：第1层数据底座（Core→Collector→分信号存储 Tempo/Jaeger·Prometheus/VM·Loki+Vector·Langfuse + 自研 Sessions→PostgreSQL + Grafana/Alerting）；第2层 AI 洞察层（Insights 引擎从各存储提炼三类洞察）。
  2. 文档 `docs/OBSERVABILITY-TARGET-ARCH.md`：现状回顾 / 设计目标 / 目标架构方案(6层) / AI 洞察提炼项(5.1性能 5.2准确率 5.3转化) / 整改路径(P0~P3) / 风险。
- 关键结论延续：Collector 加 exporter 即对接标准栈、Core 业务代码零改动；H2 单库→PostgreSQL；自研 9090 sessions 保留（标准栈无会话级语义）；LLM 404 仍为总阻塞。
- 之前几轮铺垫：名词解释(Langfuse/Grafana/Loki/ELK/Prometheus/TLS)、组件全景(Tempo/Jaeger/Zipkin/Pyroscope/Sentry/Beyla/Alloy/Vector/VictoriaMetrics)、P2/P3 Core 兜底修复(LLM失败沿用last_domain/FOLLOW/currentAgent)。

## R34 — 修复《可观测DEMO-v18-WorkBuddy.html》（基于用户 4 点反馈 + frontend-design skill）
- 用户反馈：① 日志查询空白；② 告警规则为空；③ 智能洞察项占顶部篇幅过大，6 个 TAB 被忽视；④ 总览数据健康条「已接通/未接通」混排、不知道看啥。
- 修复动作：
  1. **数据健康可信度 redesigned**：用环形进度 + 状态聚合（2 已接通 / 2 已接待上报 / 2 待接入）+ 分类清单，替代原来混排 pill 条。同时更新设置页健康卡片。
  2. **AI 洞察页重构为左-右布局**：左侧主区 6 个 TAB 成为视觉主体；右侧 320px 紧凑面板展示 9 条洞察 mini 卡，按性能/准确率/业务分类，带严重度标签 + 下钻到对应 TAB。解决"洞察占篇幅、TAB 被忽视"问题。
  3. **日志查询修复并补数据**：补全 `.log-entry/.log-time/.log-level/.log-info|warn|error` CSS，日志不再空白；新增 3 条样例（L0 命中 TRANSFER、ObsChatModel 异常、Collector 接收 batch），共 7 条。
  4. **告警规则页 redesigned**：从 3 条简单 alert-row 升级为 6 条 alert-card 规则表（LLM 错误率、P95 时延、转账槽位完成率、Collector 接收量、满意度负面、Reroute 率），带启用开关、通知渠道、严重度条、新增按钮。并修复 `itab`/`psw` 选择器，避免主 TAB 与性能子 TAB 互相清空 active。
  5. 使用 `frontend-design` skill：引入 Manrope + JetBrains Mono 字体；卡片使用更精致的阴影/悬停动效；整体配色更专业。
- 文件：`observability/frontend/可观测DEMO-v18-WorkBuddy.html`（726 行，基于 v17 复制后修改）。
- 验证：7 个 page id 齐全；尾部 `</script></body></html>` 完整；日志/告警/洞察/健康卡片均可见。

## R33 — 重新审视结论 + 产出《可观测DEMO-v17-WorkBuddy.html》+ 修正目标架构文档
- 用户要求：参考 `docs/specs/AI可观测-系统架构设计-WorkBuddy-V1.md`、`指标GAP分析-v2.md`、`可观测DEMO-v16.html`，① 重新审视之前结论；② 给更好 UI 建议并重命名为 v17。
- **重新审视三处修正（已写入 `docs/OBSERVABILITY-TARGET-ARCH.md` §1.1）**：
  1. **存储迁移不是 P0**：spec 明确 H2 P0 保留、P2 才迁 PostgreSQL；Tempo/Prom/Loki 仅信号外溢，业务语义留关系库。真正 P0 阻塞 = **Core 发射层数据断链**（DAU/置信度/分层意图/reroute，对齐 GAP v2 P0-3~P0-6）。
  2. **AI 洞察层不是独立引擎**：spec 已定义 AIInsightsService 族（6 Service）+ 前端 6 TAB（准确率/性能/漏斗/Token/工具/满意度）。Grafana 仅统一看板载体，不与 6 TAB 竞争。
  3. **Langfuse 属 spec 待定项 C**：定位/接入/部署待定，不抢占 P0/P1。
- **整改路径重排**：P0=Core 发射层补埋点（让 6 TAB 有真数据源）→ P1=9090 后端 6 TAB 真实聚合 + Grafana 告警 → P2=H2→PG + 引标准栈信号外溢 → P3=Langfuse(待定)/Pyroscope/Sentry/Beyla/TLS/模型对比。
- **新交付 UI**：`observability/frontend/可观测DEMO-v17-WorkBuddy.html`（603 行，基于 v16 强化）。核心增强：① 顶部「数据健康可信度」pill + 设置页三态角标（待接入/已接待上报/已接通）；② 总览 Zone 加 trust 角标 + 慢调用 TopN 下钻；③ 会话回放三件套互跳 + 满意度反馈；④ trace 详情 L0→L1→L2 树 + 瀑布图 + Slow/Error 标注；⑤ **AI 洞察页三类提炼看板**（性能瓶颈/准确率提升点/业务转化瓶颈各 3 卡带严重度 + 下钻），保留 6 TAB，满意度 TAB 加原因聚类；⑥ 日志三件套跳转；⑦ 告警闭环时间线（呼应 SenseNova 挂一整天痛点）；⑧ 设置页 GAP 对照表（对齐 P0-3~P0-6 待 Core 埋点项）。
- 校验：wc -l=603；page 区块 7 个平衡；尾部 `</script></body></html>` 完整。
- 任务状态：#62(v17 UI) 完成；#63(文档/记忆同步) 完成。
- **用户侧待办**：重启 Core 验证 P2/P3 兜底（即便 LLM 404，「张三」也应留 TRANSFER 走模板追问）；修复模型 key 后验证真实路由。

## R35 — 修复 v18 结构 bug + 智能洞察独立为 TAB（v19）
- 用户反馈：① 日志页/告警页还是空白；② 智能洞察改为单独 TAB 放最后一位；③ 与 AI 洞察页要有区分度。
- 根因定位：用 Playwright 无头渲染 + DOM 诊断发现 `#p-logs` 竟是 `#p-insight` 的子元素——**AI 洞察页有一个 `<div>` 未闭合**，把日志/告警/设置页全吞进去了。这是 v17/v18 一直存在的 bug，以前只是靠“总览页能看”掩盖。
- 修复动作：
  1. **修复 HTML 结构**：在 AI 洞察页正确闭合后，日志/告警/设置页成为 `#ctx` 的同级兄弟；div 标签从 586/585（不平衡）变为 595/595（完全平衡）。
  2. **智能洞察独立为新页面**：从 AI 洞察页移除右侧 insight-sidebar，提取为 `#p-intelligence` 独立页面；导航新增「洞察」分组，将「智能洞察」放在**最后一位**（所有 nav 项最末尾）。
  3. **视觉区分**：AI 洞察页保留 6 个 TAB 成为视觉主体；智能洞察页使用紫-蓝渐变头部、✨ 图标、3×3 网格卡片、彩色左边界（性能红/准确率紫/业务青）、独立悬停动效，与 AI 洞察页明显区分。
  4. **交互打通**：新增 `gotoInsightTab(name)` 函数，智能洞察卡片点击后先切到 AI 洞察页，再激活对应 TAB（如性能/准确率/漏斗）。
- 文件：`observability/frontend/可观测DEMO-v19-WorkBuddy.html`（768 行）。
- 验证：8 个 page 全部正常渲染（总览/会话/链路/AI 洞察/日志/告警/设置/智能洞察），无 JS 语法错误；div 完全平衡。
- 任务状态：#68(HTML 结构修复)、#70(智能洞察独立 TAB)、#69(验证) 均完成。

## R34 — 可观测优化总结文档（0711）
- 用户要求产出一份能直接指导落地的总结文档《可观测优化总结-0711-Workbuddy.md》，含 7 节：优化计划/目标架构(Mermaid)/各功能详细设计(Core埋点+采集+处理+存储Redis)/指标全表/DB表设计(Redis→DB同步原则)/AI优化说明/补充。
- 材料来源：Explore 子代理梳理实际代码（ObsChatModel/AgentSpanContext/SessionBridge/Collector/OtlpParserService/SessionService/AIInsightsService族/Redis用法/schema.sql 11表）+ 读 GAP v2 指标表 + OBSERVABILITY-TARGET-ARCH.md。
- 关键结论写入文档：
  1. **Redis→DB 同步原则确认保留并细化**：热窗口聚合镜像到 `redis_metrics_snapshot`（已实现30s同步）；原始遥测直接落 H2 为系统真相源；**语义比率不存 Redis 比率 key、改 H2 读时算**（GAP 中 accuracy:* Redis key 无人写即此误区）；会话/业务语义权威源在 H2。
  2. 组件选型推荐运维最省组合：**Alloy(采集)+Tempo+VictoriaMetrics+Loki+Grafana** 五件套；Langfuse 列为 P3 待定项 C。
  3. P0 待补指标契约表（llm.confidence/reroute.count/分层意图/加 layer tag）+ sessions 表加列建议（l0/l1/l2_intent/reroute_triggered）。
- 文件：`docs/可观测优化总结-0711-Workbuddy.md`（484 行）。任务 #73 完成。
- 注意：H2 `ddl-auto:none`，加列需改 schema.sql + ALTER 运行期 .mv.db（有锁风险），优先复用现有列、P2 迁 PG 时再规范。

## R35 — 补充「各组件整改前后变化」（用户反馈）
- 用户指出《可观测优化总结-0711-Workbuddy.md》缺少"各组件整改前后变化"的对照。
- 在 §2.3 之后新增 **§2.4 各组件整改前后变化**：① 组件前后对比总表（整改前现状→整改后目标态→变化点→落地阶段，覆盖 Core发射层/Collector/H2/Redis/AIInsights/前端/告警/标准栈/Langfuse/Pyroscope等）② Mermaid timeline 演进图（现状→P0→P1→P2→P3）③ 能力成熟度矩阵（9 维度×5 阶段，🟢🟡🔴⚪）。
- 关键洞察在矩阵中体现：整改前最薄弱是「日志集中检索」与「告警自动化」两项均为 🔴——即 SenseNova 挂一整天无人知的根因；P2 引入标准栈后全面到位。
- 文档现约 540 行。

## R36 — 补充「当前架构图」+ 架构变化总结（用户反馈）
- 用户希望文档同时体现"当前架构图"以便看清前后架构变化，并总结架构变化。
- 将 §2.1 拆成三小节：**2.1.1 当前架构（整改前）**Mermaid 图（红色虚线框标缺失/断链：core.log 仅落盘、accuracy:*/reroute:* 断链 key、无告警无标准栈）、**2.1.2 目标架构（整改后）**、**2.1.3 架构变化总结**（6 处结构性变化表 + 一句话前后对比）。
- 6 处结构性变化：①Core埋点补齐 ②Collector 单管道→采样多后端 ③新增标准栈(最大变化) ④日志本地→Loki集中 ⑤告警 无→自动推送 ⑥存储/Redis 定位纠偏(断链key→读时算)。
- 文档现 625 行，含 4 张 Mermaid 图。

## R37 — 导出架构 PNG 图片（用户反馈）
- 用户希望把前后架构图导出为 PNG，便于贴到汇报 PPT 中。
- 用 Playwright + mermaid@10 CDN 渲染 §2.1.1/§2.1.2 的 Mermaid 源码，生成两张高清 PNG：
  - `docs/arch-current.png`（当前架构，含 Core 断链/无告警红色虚线框标注）
  - `docs/arch-target.png`（目标架构，含 P2 标准栈绿色框）
- 已在文档中对应图例下方插入引用：`![当前架构图](arch-current.png)` 与 `![目标架构图](arch-target.png)`，实现源码与图片左右对照。
- 临时渲染脚本已清理。任务：导出 PNG 完成。

## R38 — V20 设计稿 → 总结文档 V2 刷新
- 用户要求把 V20.html 对应的设计内容刷新到《可观测优化总结-0711-WorkBuddy V2.md》。
- 严格遵循版本化规范：先 `cp` 旧 `可观测优化总结-0711-WorkBuddy.md` → V2 新文件，只改 V2，旧文件不动。
- V2 改动点（前端 v19 → v20 设计内容刷新）：
  - 范围头/配套文档引用/两处 Mermaid 节点/§2.4.1 前端行/§6.2/页脚 共 8 处 `v19` → `v20`。
  - §2.4.1 前端大屏行补充 V20 增强清单。
  - §6.2 扩展为"智能洞察建议看板 + 诊断驾驶舱"，文档化 V20 诊断驾驶舱（TOP5 优先行动/三类瓶颈卡/慢会话根因表/不满意共性表/阈值散点 chart-diag-perf）。
  - 新增 §6.4「前端大屏 V20 设计增强」：5 处取长补短对照表（总览四卡/漏斗流失画像/Agent KPI+图+工具表/满意度低分表+原因图/诊断驾驶舱）+ 保留 V19 基线优势 + V19↔V17-Codex 优劣判断。
- 文件名含空格：`docs/可观测优化总结-0711-WorkBuddy V2.md`（注意空格，Grep 工具此前对无空格正则"v19"返回空，需带上下文正则才命中）。


