# 2026-06-27 工作日志

## Dashboard 数据空白排查 & 修复

### 诊断
- **OTel Collector 未启动**: start-all.ps1 以 `-WindowStyle Hidden` 启动 Collector，失败时只打 Warning 不阻塞。手动启动后端口 4318 正常监听（PID 28556）
- **SessionBridge 断连**: Core 的 `@Async` SessionBridge HTTP POST → Backend `/api/v1/sessions` 无效果。Core 侧无 [SessionBridge] 日志（既无成功也无失败），种子脚本 36 turns 仅 1 session 入库
- **Redis 端口错配**: Backend `application.yml` 配 `redis.port: 6379`，实际 Redis 监听 16379，导致 `RedisMetricsService` 持续 WARN（不影响 session 存储）

## Dashboard 全链路修复完成

### 最终状态
- **Redis 端口**: `application.yml` 6379→16379，Backend 用 `java -jar` 启动避开 DevTools 端口冲突
- **Collector**: 发现运行在 14318（端口错），手动杀重启到 4318
- **全链路验证**: Core(8080)→Collector(4318)→Backend(9090)→Redis(16379)→Dashboard(3000)，metrics/realtime 实时数据正常
- 创建 `scripts/direct_seed.py` 绕过 SessionBridge 直灌 H2 session 数据

## 前端自动加载修复
- SessionViewerPage.jsx + TraceExplorerPage.jsx: 添加 useEffect 自动加载数据（之前需手动点「查询」）
- TraceTable.jsx: 字段名对齐 t.ttft→t.ttftMs, t.tokens→t.tokenTotal, t.status→t.statusCode

## Trace 链路追踪页面修复

### 问题
1. Trace 详情 Modal 全空 — TraceDetailVO 只有 4 个字段，前端期望 12+ 字段
2. SpanTree 树组件的 span 节点缺少 agentName/agentDesc/agentType/llmCalls
3. Trace 列表时间窗口只有 1h → 改为 24h（同 Session 修复）
4. statusCode="UNSET" → 规范化 "OK"

### 修改文件
- `dto/TraceDetailVO.java`: +userId/sessionId/intent/agentChain/ttft/ttftMs/status/statusCode/durationMs/ioInput/ioOutput/inputText/outputText/waterfallSpans 等字段
- `dto/SpanNodeVO.java`: +agentName/agentDesc/agentType/llmCalls
- `service/TraceQueryService.java`: getTraceDetail() 用 buildTraceListVO 相同逻辑补全字段；entityToVO() 设置 agentName/agentType 等；时间窗口 3600s→86400s；UNSET→OK

### Trace 种子数据
- 创建 3 条完整 Trace（转账/账单/理财），每条 L0→L1→L2 三层 Span
- L0: DomainRouter, L1: IntentClassifier, L2: TransferService/BillService/WealthConsult
- 含完整 attributes: userId, sessionId, intent, ioPrompt, ioResponse, token.*, llm.first_token_time
- 塞入 H2 spans 表，验证通过（agentChain="L0→L1→L2", ttft=180ms, SpanTree 3层树，IO卡片有数据）
- 临时文件 `scripts/SeedTraces.java` + `scripts/seed_traces.sql` 已清理

### Dashboard 指标修复
- 根因：Redis 指标数据全空（Core 未运行，无 OTel 数据流入）
- 手工注入 Redis 种子指标：requestCount=120, errorCount=3, DAU=156, online=12, L0=120/L1=45/L2=32, TTFT P50=420/P95=1150, latency P95=950ms, intentAccuracy=92% 等
- 前端字段名对齐：`dto/RealtimeMetricsVO.java` +@JsonProperty("dau") dailyActiveUsers, +@JsonProperty("realTimeOnline") realtimeOnline
- 注意：Redis 种子 key 有 TTL（120s），Dashboard 每 3s 轮询，若不刷新会回零

### AI 洞察页面修复
- 根因：`agent_performance`/`token_cost`/`tool_calls` 表为空 + `spring.sql.init.mode: always` 每次重启重跑 schema.sql
- 注入 5 条 agent_performance（L0/L1/L2 各层 Agent TTFT/TPOT）
- 注入 4 条 token_cost（按 intent 分 model 的 Token 消耗）
- 注入 4 条 tool_calls（TransferService/BillService/WealthConsult/ChatService）
- 更新 sessions 表 satisfaction_rating（2 satisfied, 1 neutral, 1 unsatisfied）
- 修改 `application.yml`: `spring.sql.init.mode: always` → `never`（防止重启清空种子数据）
- 6 个 TAB 验证通过：准确率/Agent性能/Token成本/工具调用/转化漏斗/满意度
- 第二轮修复：AgentPerformanceService 前后端数据结构不匹配（summary→kpis, byAgent→tables.agent 等）

### Trace 列表噪音过滤
- OTel 基础设施 spans 无业务属性，显示"未采集/未识别/N/A"
- `TraceQueryService.listTraces()` 过滤 agentChain="N/A"
