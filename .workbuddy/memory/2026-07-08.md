# 2026-07-08 工作日志

## 指标 GAP 分析 V2 整理
- 基于《指标GAP分析-v1.md》(2025-07-21) + 当前代码，重新梳理产出 `docs/specs/指标GAP分析-v2.md`（v1 保留不动）。
- 关键实测结论（代码侧定位）：
  - **已解决(P0)**：A4 requestCount 改用 6h 窗口(OTel 对 1m/5m/15m/6h 均 INCR)、A5 QPS=6h累计/21600 自洽、B4 errorRate 后端计算(errorCount/requestCount 均6h)。Redis 已支持多窗口 TTL。
  - **回归(V1标已实现现损坏)**：
    - A2 DAU / A3 realTimeOnline：`setDau()`/`setOnlineUsers()` 全代码库无任何调用方 → 永远 null。
    - I3/I4 + I1 渲染：前端 AccuracyTab 读 `accuracyData.trend/.rewrite/.rootCauses`，但后端 `/ai/intent-accuracy-trend` 返回 `{series}`、`/ai/confusion-matrix` 返回 `{labels,matrix,totalSamples}`、`/ai/insights` 返回 `{confidenceDistribution,extractionCompleteness,rewriteAccuracy}` → 趋势/改写表/根因卡片全空。
  - **仍开放(P0 零进展)**：C1(intentAccuracy 分层+`accuracy:intent` Redis 无写入方)、C3(reroute_rate 无写入)、T1(LLM confidence 无 VO 字段/Core未发射)、T2(分层意图无结构化)、I1(I2 分层缺失；I1 还因 Core 发 `intent_predicted`/`intent_actual` tag 但 reader 读 `intent` 键→塌缩为 OVERALL)。
  - **新增发现(N1-N6)**：L1 agent 调用无实时 INCR(仅 SpanEntity 启发式回退)、`agent.intent.accuracy` Counter 被误当置信度比率、Core 与 Dashboard 双数据通路不一致(`accuracy:intent` Redis vs `agent.intent.accuracy` H2)、5 个新 AI 洞察 TAB(AgentPerf/TokenCost/ToolCall/Funnel/Satisfaction)建议另立 GAP 审计。
- 核心洞察：语义质量/链路层 GAP 的"外部写入方"从未在代码库落地，后端长期读"没人写的 Redis key"。建议先止血回归(DAU写入+AccuracyTab契约)，再攻分层准确率架构决策。
- 工具 grep 验证法：对 `setXxx()` setter 做全仓调用方检索，可快速判定某指标是否真有数据源。

## 指标 GAP 修复（V2 落地，2026-07-08 续）
- **P0-1 DAU/实时在线（已修复）**：`RedisMetricsService` 新增 `recordDauUser(userId)`（Redis SET `obs:metrics:dau_users:{date}` 去重→SCARD 刷 DAU）与 `recordOnlineUser(userId)`（ZSET 5 分钟滑动窗口→ZCARD 刷 online_users）。接入点 `SessionService.upsertSession`（SessionBridge 每次会话交互调用；userId="unknown" 跳过）。
- **P0-2 AccuracyTab 契约（已修复）**：新增统一端点 `GET /api/v1/ai/accuracy-report`（`AIInsightsService.getAccuracyReport` 聚合 trend 按 `intent_predicted` 分组 + rewrite 表/rootCauses 从 H2 `agent.rewrite.accuracy` 派生 + confusion 复用）。前端 `AccuracyTab` 改单端点 `fetchAIAccuracyReport`，趋势 y 轴 0–100%。`mvnw.cmd compile` → BUILD SUCCESS。
- **P1-2 L1 实时计数（已补钩子）**：`OtlpParserService.incrementAgentRedis` 新增 L1 检测（intent.classif/agent.l1/l1.call/intent.l1/router.l1），Core 发射 L1 指标即计入 `agent_call:L1`；此前仍走 Span operationName 启发式回退。
- **未改动（待 Core 确认）**：P0-3~P0-6 仍依赖 Core 写 `accuracy:intent:L0/L1`、在 span/metric 携带 `llm.confidence`/分层意图等规范字段——属跨团队数据通路决策，已在 V2 文档第八节标注。
- V2 文档第八节已记录本轮修复；P0-1/P0-2/P1-2 三项任务标记完成。

## 修复后实测验证（服务重启后，主人要求自验）
- **P0-1 ✅**：`POST /api/v1/sessions`(userId=verify-user-001) 后 `GET /api/v1/metrics/realtime` 的 `dau` 0→1、`realTimeOnline` 0→1。DAU/在线数据源真贯通。残留：verify-session-001/verify-user-001 测试数据已写入(dau≥1 不自动清零，Session 列表多出该条)。
- **P0-2 ✅结构/⚠️数值**：`/api/v1/ai/accuracy-report` 返回完整 trend/rewrite/rootCauses/confusion，前端不再空白。但 trend 出现 572% 等 >100% 值→根因 H2 `agent.intent.accuracy` 存的是 Counter 累积值(≈5.72)非 0-1 比率，属 P0-3 数据语义问题(Q2，本轮未修)。
- **P1-2 ✅代码/⏳运行时**：realtime 的 `fallbackMetrics` 含 `l1Calls`(说明 `agent_call:L1`=0 走 Span 回退)，符合预期；L1 实时计数钩子已编译进后端，待 Core 发射 L1 OTel 指标方可观测实时值。
- 验证技巧：Git Bash 下 curl `-d '{"k":"v"}'` 单引号 JSON 易被吞导致 Spring 收空 body 返回 400；改用 Python `urllib` 发请求可避开 shell 引号问题。

## 第二轮修复：P0-3 代码 + C2（2026-07-08 续）
- **P0-3 intentAccuracy（代码✅/算法✅/运行时❌）**：`AIInsightsService` 新增 `computeIntentAccuracy(from,to)`（按 `(intent_predicted,state)` 取窗口内最新 Counter 累计值聚合 `correct/Σ(state)×100`）+ `buildTrend` 重构(消除 572% 失真) + `buildOverallStats` 文案改真实百分比；`MetricsQueryService` 注入 `AIInsightsService`，intentAccuracy 由"读空 Redis"改为"`computeIntentAccuracy(近6h)` 兜底"。算法级验证：用 4013 条 `agent.intent.accuracy` 记录复算→总体 76.92%、趋势全部 ≤100%。**运行时被 Core 卡住**：当前 Core(8080) 未向 H2 写 `agent.intent.accuracy`（记录数=0），且 `/api/bank/chat` 返回 HTTP 500（No converter for WorkflowOutput）致 query 在 L2 准确率埋点前中断。
- **C2 rewriteAccuracy（代码✅/运行时✅）**：同构新增 `computeRewriteAccuracy(from,to)`（H2 `agent.rewrite.accuracy` 均值×100）→ MetricsQueryService Zone C 兜底。`mvnw package` → BUILD SUCCESS。运行时验证：`rewriteAccuracy=0.0`（非 null，从 fallbackMetrics 消失），Dashboard 由"改写 -"变"改写 0%"。数据观察：实时库 28 条 `agent.rewrite.accuracy` 全为 0.0（rule 模式改写准确率确记为 0），属 Core 语义非后端 bug。
- **关键环境坑（重申）**：后端 Java 进程在 WorkBuddy 沙箱会被干掉；`mvnw package` 重打包时被运行中的 backend 锁住 jar（`Unable to rename ...jar`）→ 须先停 backend 再 package。验证后端务必用 `java -jar`（工作目录=项目根，指向 2.2GB 库）而非 spring-boot:run（后者带空格 `-Dspring.datasource.url` 会被 mvnw.cmd 截断）。
- **结论：后端可独立修复的 GAP 已穷尽**（P0-1/P0-2/P1-2/P0-3代码/C2）。剩余 P0-3运行时/P0-4/P0-5/P0-6/C3/C4/D1/D2/I2/I5 全部依赖 Core 发射对应遥测（chat 500 修复 + intent/confidence/分层意图/reroute/span属性）。V2 文档新增 8.7(C2)、8.8(剩余项 Core 前置条件表)。

## Core 侧 P0-3 链路复核（2026-07-08 续，主人确认"好"后）
- 复核 Core 发射链路，确认源码层 P0-3 数据源与导出**全通**：
  - `AbstractDomainService.recordIntentAccuracy(predicted, actual, wasDisambiguated, triggeredReroute)`(268行) 按四态判定 state(correct/disambiguated/error/fuzzy)→调用 `obsMetrics.recordIntentAccuracy(state, predicted, actual)`，发射 `agent.intent.accuracy` Counter(tag: state/intent_predicted/intent_actual)。
  - 调用方：`SingleSubAgentDomainService:208`、`MultiSubAgentDomainService:308/316/341/372/377/381`（L1/L2 执行完成时）。即只有 TRANSFER/BILL/WEALTH 等非闲聊域会发射 intent accuracy。
  - **关键约束**：`ChatService`（CHAT 闲聊域）`implements DomainHandler` 但**不 extends AbstractDomainService** → 闲聊请求不产生 intent accuracy 数据。验证 P0-3 必须用非闲聊请求。
  - 导出配置：`application.yml` `management.otlp.metrics.export.url=http://127.0.0.1:4318/v1/metrics`(step 60s)；依赖 `micrometer-registry-otlp` + `spring-boot-starter-actuator`(pom:91/95)；javaagent `-Dotel.exporter.otlp.endpoint=http://127.0.0.1:4318`(pom:118)。链路与 C2(rewriteAccuracy 已落地28条)一致，故 P0-3 必可同源落地。
  - `SseOutputAdapter.toJson()` 当前返回 `ResponseEntity<WorkflowOutput>` 且显式 `contentType=application/json` → 历史"500 No converter for WorkflowOutput"实为旧 jar；当前源码已修。
- **编译阻塞**：`mvnw.cmd clean package` 失败——`target\mobile-ai-demo-0.0.1-SNAPSHOT.jar` 被用户真实机器运行的 Core 进程排他锁住（跨沙箱 Get-CimInstance 查不到，但文件锁生效）。首次 `package`(无 clean) BUILD SUCCESS 仅 repackage 现有 classes（"Nothing to compile - all classes are up to date" 说明 classes 已含 ResponseEntity 修复），但用户当前运行的 Core 是启动时加载的旧 jar 句柄，未自动更新。
- **待办（需主人配合）**：在主人 PowerShell 窗口 Ctrl+C 停掉 Core（释放 jar 锁）→ 我侧 `clean package` 重编译 → 主人用最新 jar 重启 Core（带 OTel agent + 4318 endpoint）→ 用 WEALTH/TRANSFER 域请求打 P0-3，等 60s OTLP 步长后查后端 intentAccuracy 应非 null。

## P0-3 运行时验证通过 + 全量端点体检（2026-07-08 续，主人重启服务后）
- **P0-3 运行时 ✅ 真通！** 主人重启服务(含最新 Core jar)后，用 `test/seed.sh`+`test/seed_new.sh` 打 L2 流量(非闲聊：WEALTH_CONSULT/WEALTH_INTERPRET/TRANSFER/BILL 等)，等 60s OTLP 步长后验证：
  - `GET /api/v1/metrics/realtime` → `intentAccuracy = 73.33`（注意字段在 `data.intentAccuracy`，不在顶层，之前误读层级导致误判 null）
  - `GET /api/v1/ai/accuracy-report` → OVERALL 73.3%，分项 TRANSFER 100% / WEALTH_CONSULT 75% / WEALTH_INTERPRET 66.67% / BILL_QUERY 100% / UNKNOWN 0%（`buildTrend` 按 `intent_predicted` 分组 + correct/Σ(state) 算法正确）
  - Core `/actuator/metrics/agent.intent.accuracy` 实测 COUNT=15，tag 含 state(correct/disambiguated)/intent_predicted → 数据源确在 Core 侧。
  - **根因复盘**：之前运行时 null 是旧 Core jar(锁住未更新) + 读错 JSON 层级两层叠加；实际链路 Core→Collector(4318)→Backend(9090 H2)→computeIntentAccuracy 完全贯通。
- **死端点发现**：独立 `GET /api/v1/ai/intent-accuracy-trend` 仍返回错误值 `OVERALL准确率 1.9697`——其 `getIntentAccuracyTrend()`(222行)用旧逻辑：`extractTag(tags,"intent")`(实际 tag 名是 `intent_predicted`→全塌缩 OVERALL) + 对 Counter 累积值直接 avg(572%/1.9697 同源 bug)。但 grep 确认前端 `AccuracyTab` 已改用 `/accuracy-report`、`DashboardPage` 用 realtime → 该端点**已无人调用(死代码)**。建议修复使其与 buildTrend 一致(需 backend 重编译，jar 锁待主人停 backend)。
- **全量体检(GAP 当前真实状态)**：
  - ✅ 绿：intentAccuracy 73.33 / accuracy-report 分项 / token-cost / tool-stats / satisfaction / conversion-funnel(结构在但 L1/L2 阶段=0)。
  - ⚠️ C2 rewriteAccuracy=0.0：根因是 `agent.rewrite.accuracy` 是 **DistributionSummary**，Micrometer OTLP 导出为 histogram 时不携带每次记录的 1.0/0.0 值(sum=0)→后端存 0。非后端代码 bug，是 **Core 指标类型问题**(应改 Counter 或另发 pass 计数)才能修真值。
  - ❌ l1Calls=0：Core 仅发射 `router.decision.outcome` 的 `layer=L0`，**从不发射 L1** → P0-5/P1-2 实时 L1 计数被 Core 缺遥测卡住。
  - ❌ rerouteRate=None / violationRate=None / transferToHumanRate=None / completionRate=None / conversionRate=None：Core 无对应 reroute/business 指标写入 → C3/C4/D1/D2 全阻塞。
  - ❌ confusion matrix totalSamples=0：后端 `getConfusionMatrix` 从 Span attributes 读 `intent_predicted`/`intent_actual`，但 Core span **未携带这两个属性** → I2/P0-6 混淆矩阵空。
  - ⚠️ intent-distribution 端点返回 `{}`(但 realtime 的 intentDistribution 有值 TRANSFER:1224/CHAT:733/WEALTH:624/BILL:580) → 两处数据源不一致小问题。
  - skill-stats 接口返回 501 "P1实现"(未实现)。
- **结论**：P0-3 本轮彻底闭环(代码+算法+运行时全 ✅)。剩余所有开放项(P0-4 confidence/P0-5 L1/P0-6 分层/C3 reroute/C4/D1/D2/I2) 全部是 **Core 侧遥测缺失**(L1 发射/rewrite 改 Counter/span 带 intent 属性/reroute 与 business 指标) 或 **backend 重编译依赖**(死端点、intent-distribution 一致性)。下一步需进入 Core 遥测补齐阶段，且改 Core/backend 均需主人停进程释放 jar 锁后我重编译。

## Core 遥测补齐阶段（代码完成 + 双 jar 重编译成功，待重启验证）
- **主人配合**：用 PowerShell `Stop-Process` 停掉 Core(8080)+Backend(9090)（保留 Collector）释放 jar 锁 → 我侧 `clean package` 重编译两 jar 均 BUILD SUCCESS。
- **Core 侧改动**(src)：
  - `ObservabilityMetrics`：新增 `rewritePass` Counter + `recordRewriteAccuracy` 内 `rewritePass.increment()`；新增 `recordL1Call(type,domain)`、`recordReroute(domain)`。
  - `ContextRouter`：注入 `obsMetrics`，`route()` 返回前 `recordL1Call(routeType,domain)`（解锁 P0-5/P1-2 的 Core 侧 L1 发射）。
  - `SubGraphRouter`：`rewriteAndIdentify` 后 `recordL1Call("intent_identify",intentName)`。
  - `BankController`：两处 reroute 分支 `recordReroute(currentDomain)`（解锁 C3 前置）。
- **Backend 侧改动**(observability/backend/src)：
  - `AIInsightsService.computeRewriteAccuracy` 改读 `agent.rewrite.pass`/`agent.rewrite.total`（C2 真值，不再读失真 DistributionSummary sum=0）；新增 `computeRerouteCount`、`latestCounterTotal`（取 Counter 窗口内最新累计值求和）。
  - `getConfusionMatrix` 改读已验证的 `agent.intent.accuracy` Counter，按 intent_predicted×intent_actual 聚合二维矩阵（解锁 I2/P0-6，不再依赖 Core span 携带 intent 属性）。
  - `buildRewriteSection` 用 pass/total 算真值；`MetricsQueryService.rerouteRate` 改调 `computeRerouteCount/(l0Calls+l1Calls)×100`（解锁 C3）。
- **编译结果**：Core `target/mobile-ai-demo-0.0.1-SNAPSHOT.jar` 19:28:43 BUILD SUCCESS；Backend `target/observability-backend-0.1.0-SNAPSHOT.jar` 19:30:15 BUILD SUCCESS（首次 backend 编译报 `[415,26] long 无法转换为 Double`——computeRerouteCount 返回 `Math.round(total)` 为 long 与签名 Double 不兼容，改 `(double) Math.round(total)` 后通过）。
- **待主人重启服务加载新 jar**（start-all.ps1 或手动：先 Collector→Backend→Core），用 `test/seed.sh`+`seed_new.sh` 打 L2 流量，等 60s OTLP 步长后验证：realtime 的 `l1Calls`/`rewriteAccuracy`/`rerouteRate` + `accuracy-report` 混淆矩阵 `totalSamples>0`。

## 重启用新 jar 后的验证（沙箱自跑 verify_gap_batch.py）
- **用户 PowerShell 跑脚本无输出**：是其机器 `python3` 不在 PATH / 编码问题，非脚本问题；沙箱直接 `python3 test/verify_gap_batch.py` 正常输出完整结果。
- **验证结果**（注：H2 数据为当日上午残留，19:28 重启后用户尚未打新 seed 流量）：
  - ✅ `l1Calls=184` → P0-5/P1-2 闭环（backend 读到 H2 的 `agent.l1.call`）。
  - ✅ `rerouteRate=0.03` → C3 闭环（backend 读到 H2 的 `agent.reroute.count`）。
  - ✅ `confusion.totalSamples=10` → I2/P0-6 闭环（`getConfusionMatrix` 从 `agent.intent.accuracy` Counter 聚合成功）。
  - ❌ `rewriteAccuracy=None` → C2 仍 None。
- **H2 真因（debug/metric-names + metric-samples）**：
  - `agent.l1.call`(195条,值正常5/3)、`agent.reroute.count`(18条,值正常3) 均成功进 H2 → **证明 Core 新 jar 的 recordL1Call/recordReroute 发射点已生效**。
  - `agent.rewrite.pass` **完全不在 H2**（rewritePass 仅 ruleCheck="pass" 时 increment，改写检测逻辑全判失败 → 从不 pass）。
  - `agent.rewrite.total`(2589条) **值全 0.0** → Micrometer 对预注册 `rewriteTotal` Counter 的 OTLP 累计值导出异常（动态创建的 agent.l1.call/agent.reroute.count 导出正常，对比鲜明）。
  - → `computeRewriteAccuracy` 旧逻辑 `total<=0 return null` 触发 → C2 恒 null。
- **C2 修复（纯 backend，Core 不用重编）**：`computeRewriteAccuracy` + `buildRewriteSection` 改从 `agent.rewrite.accuracy` 的 `rule_check` tag 计数（pass 数/总数×100），与该 metric 的根因统计同源且鲁棒，不再依赖失真 Counter；有数据时必返回 0~100 真实百分比，无数据时返回 null。`test/verify_gap_batch.py` 的 C2 判定同步改为"非 None"。
- **编译阻塞**：backend 进程锁住 `observability-backend-0.1.0-SNAPSHOT.jar`，`mvnw.cmd clean package` 报 `Failed to delete ... 另一个程序正在使用此文件` → 需用户停 backend 释放锁后我才能重编译。Core jar 无需重编（C2 修复仅 backend 侧）。
