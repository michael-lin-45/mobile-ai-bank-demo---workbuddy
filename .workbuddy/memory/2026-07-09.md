# 2026-07-09 工作日志

## C4/D1 假 0.0 修复二次验证（跨越 07-08 夜）
- 主人再次执行 `start-all.ps1`（脚本第 260 行先停 9090 释放 jar 锁，第 269 行 `mvn package -DskipTests -q` 重编 backend，第 325-335 行同构重编 Core）。
- 重申结论：`start-all.ps1` **确实含重编 backend 动作**，且自己处理 jar 锁，无需手动停 backend。
- 拉运行中 backend `/api/v1/metrics/realtime` 实测：
  - completionRate=**100.0**（旧 jar 是假 0.0）→ 修正版兼容 `result=success` 真实成功源生效
  - conversionRate=**100.0** 同源同构
  - violationRate=None（D2 无数据源，按设计保持 null）
  - rerouteRate=0.02（C3 正常）
- 结论：C4/D1 已完成真值化闭环，本轮无代码改动，仅运行时验证。

## 执行 seed 做完整验收（主人要求"先跑L2，做完整验收"）
- 在沙箱 bash 直接执行 `test/seed.sh` + `test/seed_new.sh`（Core 8080 在线、curl/python3 可用）。
- **seed 实际未产生新 L2 遥测**，两个根因：
  1. **LLM 404（主因，阻断）**：所有请求 Core 返回 `聊天服务暂时不可用: 404`（`ChatService.java:70` 捕获 ChatClient 异常，空 body → "404 - "）。
  2. **UTF-8 损坏（次因）**：`seed_new.sh` 中文消息在沙箱 Git Bash（非 UTF-8 代码页）下经 `curl -d "{\"message\":\"$中文\"}"` 被损坏 → Core 侧 `JSON parse error: Invalid UTF-8 middle byte 0xe3` → 400。英文 seed 因 `content:null` 触发 python 解析 `None[:40]` 类型错 → 显示 `(sent)`（实际也走了 404 错误响应）。
- **verify_gap_batch.py 验收结果（基于 H2 历史 5827 次 L2 数据）**：l1Calls=184✅、rerouteRate=0.02✅、completionRate/conversionRate 非None✅(=100.0)、rewriteAccuracy=0.0✅、confusion.totalSamples=9✅、关联端点全 OK。**指标修复代码路径已验证正确**，但本轮 seed 未补充新流量。

## 根因定位：Core LLM 404 是 base-url 多写一层 /v1（非网络问题）
- 逐步排查：直连 MaaS `.../compatible-mode/v1/chat/completions`（带 key）直连/走代理均返回 **200**；而 `.../v1/v1/chat/completions` 与 `.../chat/completions`(无/v1) 均空 404 → 与 Core 的 "404 - " 吻合。无 `DOMAIN_*`/`*_BASE_URL` 环境变量覆盖。
- **决定性证据**：反编译 `spring-ai-openai-1.1.2.jar` → `OpenAiApi$Builder` 把 `completionsPath` **硬编码为 `/v1/chat/completions`**（`ldc #24 // String /v1/chat/completions`）。最终 URL = `baseUrl + "/v1/chat/completions"`。
- 而 `application.yml` 6 个 `models.*.base-url` 都带 `/v1`（`.../compatible-mode/v1`）→ 实际打到 `.../compatible-mode/v1/v1/chat/completions` → **404**。之前 5811 次成功调用是因为当时 base-url 无 `/v1`（Spring AI 自动拼出正确路径）。
- **修复（纯配置，零风险）**：将 6 个 base-url 的 `/v1` 去掉 → `.../compatible-mode`。Spring AI 拼出 `.../compatible-mode/v1/chat/completions` → 200。已改 `src/main/resources/application.yml`（grep 校验 6 行均已去 /v1）。
- **待主人操作**：重新执行 `start-all.ps1`（自动 `mvn package` 重编 Core 把新 yml 打进 jar 并重启）。重启后我再用 `test/seed.sh`+`seed_new.sh`（沙箱内对中文用 `chcp 65001`/UTF-8 locale 避免损坏）打 L2 流量，最后跑 `verify_gap_batch.py` 做基于新流量的完整验收。

## 完整验收已完成（基于新 L2 实时流量，真实验收闭环）
- 用户已重启 `start-all.ps1`（含新 yml base-url 修复）。重测 Core：`POST /api/bank/chat` 英文消息返回 `COMPLETED` 正常响应 → **404 根因消除** ✅。
- 编写 `test/seed_all.py`（UTF-8 安全：urllib + utf-8 body），替代 Git Bash 下中文会 UTF-8 损坏的 `seed_new.sh`；覆盖原 `seed.sh`(19 条英文) + `seed_new.sh`(17 条中文) 全部 36 条消息。沙箱 managed python 直连 `127.0.0.1:8080` 执行。
- 36 条全部正常路由（`[COMPLETED]`/`[DISAMBIGUATION]`/`[INTERRUPTED]`），**无 404、无 400 编码错误**，真实 L2 流量产生。
- 等待 60s OTLP 步长后 `verify_gap_batch.py` 真实验收（对比基线 5853→5884）：
  - `l2Calls` 5853→**5884**(+31)、`l1Calls` 184→**238**(+54)、`dau` 8→**15**、混淆矩阵 `totalSamples` 9→**27**(+18)
  - C4 `completionRate`=100.0(非None)、D1 `conversionRate`=100.0(非None)、C2 `rewriteAccuracy`=0.0(非None)、C3 `rerouteRate`=0.03、P0-5/P1-2 `l1Calls`>0 全 ✅
  - 关联端点 `intent-distribution`/`tool-stats`/`token-cost`/`satisfaction` 全 OK
- **结论**：所有 P0/P1 GAP 指标（A6/C1/C2/C3/C4/D1/I1/I2/P0-3/P0-5/P0-6）已真值化闭环。本轮为基于新 L2 实时流量的完整验收，非历史数据。
- 已知语义局限（已在 GAP 文档记录）：`completionRate` 恒 100% 因 Core 仅埋 `result=success`、无 `result=fail` 埋点（INTERRUPTED 中断对话不发 outcome）；D2 `violationRate` 无安全围栏数据源，保持 null（前端显示"暂无"）。

## 死端点清理已完成（收尾，2026-07-09）
- 删除三项确属死端点（grep 确认前端无组件调用）：
  1. `/intent-accuracy-trend`：旧 `{series}` 结构，前端已改 `/accuracy-report`。删 `AIInsightsController`+`AIInsightsService` 的 `getIntentAccuracyTrend` + 前端 `fetchIntentAccuracyTrend`。
  2. `/skill-stats`：`@Deprecated` 空壳返 501。删整个 `SkillStatsController.java`。
  3. `/intent-distribution`：与 realtime 不一致根因 = Core `recordIntentRecognized()` 的 `intentRecognized.increment()` 未带 intent tag（ObservabilityMetrics.java:308）→ 聚合空/失真；前端无组件消费。删 `getIntentDistribution`（controller+service）+ 前端 `fetchIntentDistribution`。（`MetricsQueryService`/`RedisMetricsService` 同名方法是 realtime 另一链路，保留。）
- 删死 DTO `IntentAccuracyVO.java`（无任何引用）；`verify_gap_batch.py` 关联端点 `intent-distribution`→`confusion-matrix`。
- `mvnw -o compile -DskipTests` → BUILD SUCCESS。待主人 `start-all.ps1` 重编 backend 部署后验收（三端点 404、accuracy-report/confusion-matrix 正常）。

## 死端点清理最终验收通过（主人"启动了"=重跑 start-all.ps1 部署新 backend）
- 验收结果（运行中 backend 实测）：
  - backend 健康 `/health` → UP ✅
  - **三个死端点全部 404** ✅：`/api/v1/ai/intent-distribution`、`/api/v1/ai/intent-accuracy-trend`、`/api/v1/ai/skill-stats`
  - **五个在用端点全部 200** ✅：`/accuracy-report`、`/confusion-matrix`、`/tool-stats`、`/token-cost`、`/satisfaction`
  - `verify_gap_batch.py` 全绿：l1Calls=368✅、rewriteAccuracy/rerouteRate/completionRate/conversionRate 非None✅、混淆矩阵 totalSamples=27✅、关联端点全 OK
- **结论**：死端点清理收尾完成，无回归，其余功能正常。指标 GAP 真值化 + 死端点清理整体闭环。

## 剩余开放项（用户未要求，非阻塞）
- P0-4/T1 LLM confidence：Core 未发射 `llm.confidence` span 属性。
- P0-6 分层趋势图前端展示：混淆矩阵数据已通，但分层（L0/L1/L2）趋势图前端未实现。
- D2 violationRate：无安全围栏数据源，保持 null。

## 第五轮验收：发现 Token 兜底仍失效（2026-07-09 续）
- 主人重启服务后实测：AGENT性能(532 calls/KPI/箱线图/散点图全有)✅、TOKEN成本(trend 312874 input/9261 output/cost 0.68 + detail 按 intent×model)✅ 均已闭环（修复③验证通过）。但 **总览大屏 Token 调用量/输入/输出仍 0** ❌、TTFT 仍 0。
- **诊断根因**：
  1. Token 兜底在 `MetricsQueryService.getRealtime` 的 60s 节流块内 → 大屏每 3s 轮询，多数轮询落在节流窗口内 → 显示 Redis 1m 值(空闲=0)，反复闪 0。
  2. `getLatestMetricValue` 取**最后一行**，但 Core 重启使 cumulative 计数器归零 → 最后一行只是重启后小计(25752)，而非真实累计总量(312874，=窗口 MAX)。TOKEN成本用 MAX 故正确显示 312874，realtime 用末行故错。
  3. `debug/metric-samples` 拉 `llm.token.input` 全 866 行：值波动(173767→16881→25752)，证实非单调累计(重启归零)。末行=25752，窗口 MAX=312874。
- **修复**：Token/Error 兜底移出 60s 节流（改为 Redis 为 0 时每次实时查 H2 MAX，廉价查询；并标记 fallbackMetrics）、`getLatestMetricValue` 改为 `getMaxMetricValue`（窗口 MAX）。前端 Zone B "Token 调用量"=tokenInput+tokenOutput，故修 tokenInput/output 即同时修三项。
- **编译/部署**：`mvnw.cmd -o package` 在 backend 运行时**失败于 repackage 锁**(`Unable to rename ...jar to ...jar.original`)——运行中的 backend 持 jar 锁；但 `compile` 阶段通过 → 代码无误。需主人停 backend 释放锁后重编。已准备 `test/send_l2_traffic.py`（向 Core:8080 发 7 会话/8 轮 L2 请求，含 transfer/bill/wealth/chat）用于重启后端后打流量验证 TTFT。
- **待主人操作**：重跑 `start-all.ps1`（停 backend 释放锁 + 重编 + 重启，含新 MetricsQueryService）→ 我侧跑 `send_l2_traffic.py` 打 L2 流量 → 等 60s OTLP + 30s TTFT 快照 → 复核 realtime token(应=312874级非0)/TTFT(应非0)/AGENT性能/TOKEN成本。

## 第六轮：链路追踪页对齐 DEMO-v16 + H2 底表排查（2026-07-09 续）
- **验收三类归零**：Token 已闭环（tokenInput=173767/Output=23561 非0）；TTFT 详情可算(12ms)但列表为 None；AGENT性能/TOKEN成本有数据。
- **链路追踪页根因（最深）**：`OtlpTracePayload.Span.extractAttributesJson()` 手写 JSON 拼接，**只转义 `\` 和 `"`，未转义换行符** → LLM 的 `ai.io.prompt` 多行文本生成**非法 JSON** → 后端 `parseAttributes`(Jackson) 解析失败 → 返回空 map → **所有自定义属性(ai.io.prompt/response、ai.token.input/output、model.name、agent.*、intent)全部丢失**。前端看到的 ioPrompt=Y 仅是 `enrichBusinessSpans` 从 SessionTurn 回填的同一条 userMessage/aiResponse（非各 LLM 真实 prompt）。
- **修复**：`OtlpParserService.parseTraces` 改为用注入的 `ObjectMapper` 安全序列化属性(`buildAttributesJson`)；`TraceQueryService.parseTokenBreakdown` 改读 `ai.token.input/output`（Core 实际 emit 的键，原读 system/context 导致 token 永远 null）；新增 `TokenBreakdownVO.inputTokens`；`buildTraceListVOFast` 改为从业务 span 聚合 TTFT(`computeTTFT`)与 token 总量(`aggregateTokenTotal`)；`getTraceDetail` 总耗时取 HTTP 根 span(列表一致)、瀑布基准取整 trace 最早开始时间(HTTP 入口条从0开始、TTFT 虚线对齐)。
- **前端**：`TraceTable` 补「意图」列(IntentBadge，DEMO-v16 在 User/Agents 间)；`SpanTree` 让 L1+每个 LLM 「详情」按钮**始终显示**，LLM token 数从 `span.attributes[ai.token.input/output]` 填充(原硬编码空串)，IO 空时给「（无记录）」提示；Agent 头部补 token 小参数。
- **H2 底表排查结论**：数值指标(Histogram/Sum/Gauge)全部经 `saveMetric`→`metricsAgg` 落 H2（已有底表，TOKEN成本读到312874 为证）；缺口仅在 span 自定义属性（已随上述修复落 `spans.attributes`）。→ 修复后所有指标均有 H2 底表。
- **双写策略评估（用户问）**：当前已在 OTLP 摄入边界 `OtlpParserService` 实现双写(Redis热层+H2温层)，**方向正确但应集中在摄入层，不应在 RedisMetricsService 各 Redis 写入点散落 H2 写**（避免业务耦合+锁竞争）。用户设想合适且已落实，无需改动 RedisMetricsService。
- **待用户操作**：`mvnw package` 受运行 backend 的 jar 锁限制无法重打包；需用户重跑 `start-all.ps1`（停 backend 释放锁→重编→重启，含新 OtlpParserService/TraceQueryService）→ 我侧跑 `send_l2_traffic.py` 打流量→等60s OTLP→复核：列表意图/TTFT/Token 均有值、详情弹窗各 LLM 显示真实 prompt/response+token、瀑布图 E2E+TTFT 虚线对齐。

## 第七轮：按路由架构规范精修 trace 详情树 + 瀑布图（2026-07-09 续）
- 用户补充 Agent 路由架构语义：L0=领域路由(1 LLM 选 L1)；L1=业务路由(含 L1-LLM1 上下文分类识别 follow-up/switch-new/resume + L1-LLM2 意图改写识别选 L2，**L1-LLM2 可选，follow-up 时跳过**)；L2=业务执行(1~N LLM)；reRoute=L0/L1 选错时 L1/L2 退回 L0 重路由。
- **前端 SpanTree.jsx 重写**：`buildAgentTree` 改为「同层级合并为单一 Agent」逻辑，按用户规范标注每个 Agent/LLM 角色说明(L0 领域路由/L1 业务路由/L1-LLM1 上下文分类/L1-LLM2 意图改写+识别/L2 业务执行)；新增 `isFollowUp`(L1 无 L1-LLM2→标「FOLLOW-UP ↪ 跳过 L1-LLM2」徽标)、`hasLLM2` 标记；LLM 行补 `desc` 角色说明；`SpanTree` 新接收 `reRouted/reRoutePath` 透传，顶部显示「↻ 重路由」横幅。
- **前端 WaterfallChart.jsx**：新增 E2E 总耗时条(占满时间轴)，满足用户「画出 E2E 和每个 SPAN 的耗时」要求；TTFT 黄色虚线 + 按 startMs 时间先后排序已具备。
- **前端 TraceDetailModal.jsx**：`<SpanTree>` 透传 `reRouted/reRoutePath`。
- **后端 TraceDetailVO.java**：新增 `reRouted`(boolean)/`reRoutePath`(String) 字段。
- **后端 TraceQueryService.java**：`getTraceDetail` 统计一条 trace 内 L0 span 数 >1 → `reRouted=true`，`buildFullAgentChain` 生成含回环「↻」的分层路径(如 `L0 → L1-LLM1 → L1-LLM2 → L2 ↻ L0 → L1 → L2`)；瀑布 label 精修为「L0 领域路由·LLM选L1 / L1-LLM1 上下文分类 / L1-LLM2 意图改写+识别 / L2 业务执行」。
- **注意**：reRoute 目前仅后端标 flag+路径横幅；`spanTree`/`waterfallSpans` 仍只含最后一次成功链(`extractLastAgentChain` 折叠首次失败链)。若需在前端渲染完整失败链 span，需改 `extractLastAgentChain` 返回全量 span——本轮未做(降低风险)，待用户确认是否要全量回放。
- 待用户重跑 start-all.ps1 部署(后端 jar 锁需停 backend 释放)→ 我侧打 L2 流量 → 复核详情树角色标注/follow-up 徽标/瀑布图 E2E+TTFT 虚线/reRoute 横幅。

## 第八轮：验证部署闭环 + 修复 2 个链路追踪显示 bug（2026-07-09 续）
- 用户"已重启服务"。实测：backend `/health` UP、frontend 3000 / Core 8080 均 200。
- **跑 `test/send_l2_traffic.py` 打 8 轮 L2 流量**(transfer/bill/wealth/chat)，Core 完整跑 L0/L1/L2 链路识别意图正常。等 50s OTLP 摄入后查 API：
  - **新代码已生效（根因修复闭环）**：列表 `intent`(如 "WEALTH → WEALTH → WEALTH")、`tokenTotal`(3112/6189/...) 均有值（旧代码因 extractAttributesJson 丢属性会为 None）；详情 `ioInput` 显示真实 LLM system prompt、`spanTree` 的 L0/L1 `io=Y` 且 `tokenInput=2865/206`（ai.token.input 已落 H2）。✅
  - **列表 TTFT 列**：前端读 `t.ttftMs`，后端 `buildTraceListVOFast` 已 set → 显示 4/4/5ms（非 None）。✅
- **发现并修复 2 个显示 bug（后端，需再重启部署）**：
  1. **瀑布图重复/错标 E2E**：`flattenForWaterfall` 把 HTTP 根 span(SERVER /api/bank/chat) 也当作业务 span 加入 → 生成一条被错误标成 "L0 领域路由 · LLM选L1"(dur=整 trace 1080ms) 的行，而前端 `WaterfallChart` 自己已画 "E2E 总耗时" 条 → 重复且错标。修复：从 `waterfallSpans` 排除 HTTP 根 span（前端 E2E 条保留为唯一定义）。
  2. **TTFT 语义错误（4ms 误导）**：原 `computeTTFT` 用「最后一个 L0 span 开始 − HTTP 根开始」= 仅"到首个路由 LLM 的耗时"(4ms)，非真实首 Token 时延（用户可见 token 来自最深的执行 LLM，远在数百 ms 后）。修复：`computeTTFT` 改为取**层级最深(L2>L1>L0)且开始最晚**的业务执行 span 的 start − 根 start，作为"答案首 Token 时延"近似（如 TRANSFER 应为 ~650ms 而非 4ms）。新增 `layerRank()` 辅助。无 Core 改动。
  - 顺带：列表 `buildTraceListVOFast` 原仅 set `ttftMs`；`TraceListVO` 无 `ttft` 字段，故未加 `setTtft`（保持与前端一致）。
  - javadoc 注释含 `*/`(在 `L1-LLM*/llm:`) 导致编译 premature comment-close 报错 → 改文案后 `mvnw.cmd -o compile` **BUILD SUCCESS**。
- **前端 SpanTree 自验证**：确认其消费 `ioPrompt`/`ai.token.input/output`/`reRouted`/`reRoutePath`/follow-up 徽标/每 LLM「详情」按钮，与后端数据结构一致，无需改动。
- **待用户操作**：再跑一次 `start-all.ps1` 部署新 backend（jar 锁需停 backend）。部署后**无需重新打流量**——已落 H2 的 span 数据即可反映新逻辑；我侧直接重查已存在的 trace(如 cbc72190...)复核：`ttft` 应≈650ms(而非4ms)、`waterfallSpans` 首行应为 "L0 领域路由·LLM选L1 / qwen3.6-35b-a3b"@off=4（无错标 HTTP 根行）。
- **仍开放项（用户未拍板）**：reRoute 渲染深度——当前为「横幅+路径」(已部署)；若要前端全量回放失败链 span 需改 `extractLastAgentChain` 返回全量，待用户决定。本次测试流量未触发 reRoute 场景。

## 第九轮：第八轮 2 个后端修复最终验收通过（2026-07-09 续，用户"重启了。先验证，reroute稍后再定"）
- 用户重跑 start-all.ps1 部署新 backend。实测三服务在线(backend 9090/health=200、frontend 3000=200、Core 8080=200)。
- **修复①验证：TTFT 语义正确**。直接查已落 H2 的 trace（无需重打流量）：
  - TRANSFER `cbc72190...`：`ttft=650`（旧为 4ms）✅ ＝ 请求开始 → 最深层执行 LLM(L1)开始(650ms) 的"答案首 Token 时延"近似。
  - WEALTH `84298e345d52...`：`ttft=2192`、`durationMs=2556`✅ ＝ 指向最晚开始的 L2 业务执行 span(off=2192)。
  - 列表 TTFT 列全部变合理值：650 / 2192 / 1221 / 2360 / 2154 / 2388 / 2296 / 3876（不再误导性的 4ms）。✅
- **修复②验证：瀑布图无重复/错标行**。
  - TRANSFER `waterfallSpans` 仅 2 行业务 span：`L0 领域路由·LLM选L1`(641ms@off4) + `L1 业务路由`(426ms@off650)，**无错标 HTTP 根行**✅（E2E 总耗时条由前端 `WaterfallChart` 唯一定义）。
  - WEALTH `waterfallSpans` 干净 5 行、角色标注正确：`L0 领域路由·LLM选L1`(527@4) → `L1-LLM1 上下文分类`(436@534) → `L1-LLM2 意图改写+识别`(853@972) → `L2 业务执行`(356@1834) → `L2 业务执行`(347@2192)。✅ 证明第七轮路由架构语义标注 + 第八轮去重全部生效，`spanTree` 同样呈 L0→L1-LLM1→L1-LLM2→L2→L2 全链且各 LLM `io=Y`(真实 prompt 落 H2)。
- **列表其余列**：intent 链(CHAT→CHAT / WEALTH→WEALTH / TRANSFER→TRANSFER×4...)✅、tokenTotal(3112/6189/5600/6682...)✅、status 状态点✅ 均有值。
- **结论**：链路追踪页对齐 DEMO-v16 的四项需求（列表列、详情弹窗真实 LLM IO+token/小参数、span 瀑布图 E2E+TTFT 虚线、H2 底表+双写策略）全部闭环验证通过。reRoute 保持「横幅+路径」现状，渲染深度待用户后续拍板（本轮未触发 reRoute 流量）。
- 本轮无代码改动，纯运行时验收。

## 第十轮：总览大屏 8 项人工验证问题修复（2026-07-09 续，Langfuse 已决定先不上）
- 用户回到总览大屏，提 8 项人工验证发现的问题。逐项根因 + 修复：
  1. **清 H2 老数据只留 2 天**：新建 `AdminController.POST /api/v1/admin/purge?days=2`（清理 spans/metrics_agg/redis_metrics_snapshot/logs，不清理 sessions/session_turns）；`MetricsAggRepository.deleteByTimestampBefore`/`LogRepository.deleteByTimestampBefore`/`SpanRepository.deleteRootSpanStartTimesSince` 等已具备。realtime H2 回退窗口 `7*86400`→`2*86400` 对齐。
  2. **首Token时延=0ms**：`OtlpParserService` 原 `recordTTFT((long)avg)` —— `avg` 是 Micrometer 基础单位「秒」，`(long)0.13=0` 截断 → 改 `recordTTFT((long)(avg*1000))`。TTFT ZSET 入库单位统一为毫秒。
  3. **P95系统时延=0ms**：同源，`recordLatency("1m",avg)` → `avg*1000`；**补一致性**：`5m/15m` 窗口此前漏改（仍秒），本次一并改为 `avg*1000`（所有窗口统一毫秒）。前端 `Math.round(p95Latency)` 现收到 ~131ms → 显示 131ms。
  4. **错误率只留2位小数**：`RealtimeMetricsVO.errorRate` 是分数(0.0012=0.12%，VO 注释确证) → 前端原 `错误率 {errorRate}%` 显示成 "0.013767%" 比例错。改 `(errorRate*100).toFixed(2)%` → "1.38%"，既2位小数又修正比例。
  5. **业务转化率改为"暂无"**：后端 `MetricsQueryService.getRealtime` 设 `businessConversionRate=null`/`conversionDetail=null`（依赖外部业务系统，Core 无埋点）；前端 业务转化率卡加 `empty={conversionRate==null} emptyText="暂无"`（仿违规率卡）。
  6. **访问用户量=0 人**：前端原用 `metrics.activeSessions`(=0)。DAU=27 才是历史统计人数 → 卡 value 改 `metrics.dau`，标签 "访问用户量 (DAU)"，sub 显示 `实时在线 X 人`（同时覆盖 #7 展示）。
  7. **实时在线=0**：后端 `RedisMetricsService.setOnlineUsers` TTL 30s 短于 SessionService 5min 滑动窗口 → 两次会话间过期归零。改 TTL 300s 匹配。前端已在 访问用户量 sub 展示 `realTimeOnline`，部署后应有值。
  8. **请求&Token趋势图例为空**：根因 `<TrendChart>` 未传 `data` prop（TrendChart 无数据时早退不渲染 legend）。修复：① `client.js` 加 `fetchMetricsTrend(hours=6)`→`GET /metrics/trend`；② 新建 `TrendVO{times,requests,tokens}` + `MetricsQueryService.getTrend(hours)`（按30min分桶：请求量=根span计数、Token=llm.token.input/output累计增量、处理重启归零）；③ `MetricsQueryController.GET /trend`；④ 前端 `DashboardPage` 加 `trend` state + `loadTrend`(30s轮询) + `<TrendChart data={trend}/>`。
- **编译/构建**：后端 `mvnw.cmd -o compile` BUILD SUCCESS（修掉 `MetricsQueryController` 缺 `import TrendVO`）；前端 `vite build` EXIT=0 全 JSX 通过。
- **待用户操作**：重跑 `start-all.ps1`（停 backend 释放 jar 锁→`mvn package` 重编→重启，含全部新代码）→ 部署后调 `POST /api/v1/admin/purge?days=2` 清老数据 → 复核 8 项闭环（重点：TTFT/P95非0、错误率1.38%、转化率"暂无"、访问用户量=DAU、实时在线非0、趋势图例有"请求量/Token"）。
- **注**：本轮前端错误率/转化率/访问量/趋势 4 项改动 + 后端 TrendVO/AdminController/MetricsQueryService(OtlpParser单位)/RedisMetricsService(TTL) 全部写完并通过编译构建，但尚未运行时验证（沙箱会杀长时 Java 进程）。

## 第十一轮：第十轮 8 项修复的收尾 + 验收脚本（2026-07-09 续，用户"继续未完成的任务"）
- **修复编译阻塞**：`mvnw package` 报 `MetricsQueryService.java:[217,11] 找不到符号 setConversionDetail` —— 第十轮写 `vo.setBusinessConversionRate(null); vo.setConversionDetail(null);` 但 `RealtimeMetricsVO` 无 `conversionDetail` 字段/setter（只有 `businessConversionRate`）。删掉 `setConversionDetail(null)` 一行 → `mvnw.cmd -o compile` BUILD SUCCESS ✅。`MetricCard` 确认支持 `empty`/`emptyText` prop（#5 用法正确）。
- **repackage 锁**：`mvnw package` 的 Spring Boot repackage 步报 `Unable to rename ...jar to ...jar.original` —— 运行中旧 backend 持 jar 锁。此锁由 `start-all.ps1`（先 `Stop-PortOccupier 9090` 释放锁再 `mvn package`）处理，**非代码问题**。`compile` 已通过，证明新代码可编。
- **实测运行后端仍是旧 jar**：直接拉 `/realtime` → `conversionRate=100.0`、`ttftP*=0`、`p95Latency=0.131`(秒)、`/trend` 返回 `http=000`(超时/无端点)。三项证据确认新修复未部署，需用户重跑 start-all.ps1。
- **验收脚本 `test/verify_dashboard.py`**（新建，纯 stdlib，可 `--purge/--traffic/--wait`）：覆盖 8 项验收点 + 预检。预检强化为「检测新 jar」：`/trend` 返回 200 且 `times` 为 list 才算新 jar；否则结合 `realtime.conversionRate != null` 判定旧 jar 并 exit 2（原预检把超时 code=0 当成功，已修）。#2/#3 阈值收紧为 `>=1`：新 jar 单位 ms(普遍>1)，旧 jar 秒(0.131→前端 0ms) → 能正确区分单位修复。实测旧 jar 下脚本正确 exit 2 并打印「请重跑 start-all.ps1」。
- **前端部署**：`start-all.ps1` 前端用 `npm run dev`(Vite HMR)，DashboardPage.jsx 改动启动后自动生效，无需单独 build。
- **待用户操作（唯一阻塞：部署）**：在 PowerShell 重跑 `.\scripts\start-all.ps1`（停 backend 释放 jar 锁→`mvn package` 重编→重启 backend/Core/Frontend）。部署后运行验收：
  `python test/verify_dashboard.py --purge --traffic --wait 120`
  （先清 2 天老数据、再打 8 轮 L2 流量、等 120s OTLP+Redis 快照后逐项断言）。预期全绿：TTFT/P95 非0(ms)、错误率 1.36%(2位小数)、转化率 null→"暂无"、访问用户量=DAU(27)、实时在线>0、趋势 buckets>0 且 请求/Token 合计>0。

## 第十二轮：8 项验收 2 FAIL + 2 隐藏 bug 修复（2026-07-09 续，用户"已重启"）
- 用户重跑 start-all.ps1 部署新 backend（第十轮全部修复已打进 jar）。实测新 jar 已生效（`/trend` 200+数据、conversionRate=None、p95Latency=304.75ms 非旧秒值）。跑 `verify_dashboard.py --purge --traffic --wait 120` 结果：#5/#6/#7/#8 通过；**#1 purge=HTTP 500 FAIL；#4 errorRate=44.0(实为 3.67=366%) FAIL**；#2/#3 虽 PASS 但值 532000ms 是**隐藏 bug**（×1000 误放大）。
- **#4 根因（errorRate 比例失真）**：`RedisMetricsService` 的 `incrErrorCount` 在 `OtlpParserService` 中对**每一个名称含 "error" 的 OTLP 指标 data point（每次导出）**自增，而非按"错误事件"自增；`incrRequestCount` 同理对 5 个 request/intent.recognized 指标每次导出自增。→ errorCount(44)=导出次数、requestCount(12)=导出次数，二者口径不一致，比值 3.67（366%）无意义。实测 `llm.error.count` 是唯一 error 指标(cumulative=42)。
- **#1 根因（purge 500）**：`void deleteByStartTimeBefore/deleteByTimestampBefore` 是 Spring Data JPA **派生删除**——先 SELECT 全表加载所有匹配实体进持久化上下文，再逐条 delete。2.2GB `spans` 表数百万行 → OOM / 长事务锁 → HTTP 500（Spring 默认吞掉异常 message，仅返 500）。4 个 repo 同病。
- **#2/#3 隐藏根因（532000ms）**：第十轮为修"0ms"把 `avg*1000`  blanket 应用，但 `avg=sum/count` 单位**因指标而异**：`http.server.request.duration`/`http.client.request.duration` 由 Micrometer OTLP 以**秒**导出（实测 0.25s / 19.86s），而 `llm.*` Timer（`ObsChatModel.buildTimer` 用 `MILLISECONDS` 记录）`avg` 本就是**毫秒**（实测 807/948）。→ blanket ×1000 把 llm.* 毫秒放大 1000 倍 = 532000ms。
- **修复（本轮，7 文件）**：
  1. `OtlpParserService`：新增 `isSecondsUnitMetric()`（仅 `http.server.request.duration`/`http.client.request.duration` → ×1000），统一算 `valueMs`；`isLatencyMetric` 排除 `jvm./tomcat./system./process.`（避免 GC 时长污染 P95）；`recordLatency`/`recordTTFT` 均用 `valueMs`。→ 单位正确：http.*→ms、llm.*→ms。
  2. `SpanRepository`/`MetricsAggRepository`/`RedisMetricsSnapshotRepository`/`LogRepository`：派生 `void deleteByX` 全改为 `@Modifying @Query("DELETE FROM ... WHERE ... < :before")` 批量删（返回 int，不加载实体）→ 根除 purge OOM/500。
  3. `AdminController.purge`：新增 `safeDelete(String, IntSupplier)` 逐表隔离，任一表失败返回 -1 不 500；返回 `deletedSpans/deletedMetricsAgg/deletedSnapshots/deletedLogs` 计数 + `purged` 布尔。
  4. `SpanRepository`：新增 `countByTimeRange` / `countErrorsByTimeRange`（带 idx_spans_time 索引）。
  5. `MetricsQueryService.getRealtime`：errorRate 改 `getErrorRateFromSpans()` = 错误数/操作数（6h 窗口），错误数 = MAX(ERROR-status span 数, H2 `llm.error.count` 窗口 MAX)；恒 0~1 分数，60s 节流缓存；前端 `errorRate*100` → 2 位小数百分比。彻底脱离失真的 Redis 计数器。
  6. `test/verify_dashboard.py`：`post()` 默认超时 15→60s（避免大表 bulk delete 超时误判）。
- **编译验证**：`mvnw.cmd -o compile` **BUILD SUCCESS**（两次，含错误率 refine）。`package` 仍只卡在 repackage 重命名 jar（运行中旧 backend 持 jar 锁）——`start-all.ps1` 先停 backend 释放锁再 `mvn package`，非代码问题。
- **待用户操作**：重跑 `.\scripts\start-all.ps1`（停 backend 释放 jar 锁→`mvn package` 重编→重启）→ 运行 `python test/verify_dashboard.py --purge --traffic --wait 120`。预期：#1 purge 200+计数；#2 TTFT~几百ms；#3 P95~几百ms~低秒（非 532000）；#4 errorRate 分数→显示 X.XX%；#5/#6/#7/#8 维持通过。
- **注**：若错误率显示 0.00%，系 6h 窗口内无 ERROR-status span 且 `llm.error.count` 为 0（真实无错），仍满足"2 位小数"要求；若想让错误率反映 Core 错误事件，需 Core 将异常标到 span status=ERROR（当前 `MetricSdk`/agent 可能优雅吞掉异常未标 ERROR）。

## 第十三轮：#1 假绿修复——@Modifying 漏 @Transactional 致 bulk delete 未生效（2026-07-09 续，用户"启动了"）
- 用户重跑 start-all.ps1 部署（含第十二轮全部修复）。跑 `verify_dashboard.py --purge --traffic --wait 120` → **8/8 全 PASS**，TTFT/P95=~388/1656ms（单位修复生效）、errorRate=0.0（分数口径）、conversionRate=None、dau=6、online=6、trend 12桶/381请求/215797 Token。
- **但发现 #1 假绿**：#1 输出 `spans=-1 metrics_agg=-1 snapshot=-1 logs=-1` —— `-1` 是 `AdminController.safeDelete` 捕获异常后的返回值，说明 **4 个表 bulk delete 全部抛异常、老数据根本没删**，仅因 HTTP 200 + code=0 让脚本误判通过。
- **根因**：第十二轮把 `void deleteByX` 改成 `@Modifying @Query(...)` 批量删，但**漏加 `@Transactional`**。这些方法继承 `SimpleJpaRepository` 类级 `@Transactional(readOnly=true)`，在只读事务里执行 DELETE → Hibernate/JDBC 抛异常（只读事务不允许写 / TransactionRequired）→ 被 safeDelete catch 返回 -1。删除从未执行。
- **修复（本轮，4 文件 + 脚本）**：
  1. `SpanRepository`/`MetricsAggRepository`/`RedisMetricsSnapshotRepository`/`LogRepository`：各 `@Modifying deleteByX` 加 `@Transactional`（写事务，覆盖类级 readOnly），import `org.springframework.transaction.annotation.Transactional`。→ bulk delete 真正生效。
  2. `test/verify_dashboard.py` #1 断言加强：原只判 `HTTP 200 + code==0`；改为**任一表 `deletedX == -1` 即判 FAIL**（防再次假绿），并展示真实删除行数。
- **编译验证**：`mvnw.cmd -o compile` BUILD SUCCESS（含 @Transactional 改动）。
- **待用户操作（唯一阻塞：部署）**：重跑 `.\scripts\start-all.ps1`（停 backend 释放 jar 锁→`mvn package` 重编→重启）→ 运行 `python test/verify_dashboard.py --purge --traffic --wait 120`。**本轮我必须主动轮询脚本直到拿到 8 项结果再汇报**（主人长期记忆强制：不得依赖被动"跑完通知"，须起定时器等待确认拿到回复后关闭对话）。预期：#1 `已清理 | spans=N metrics_agg=M snapshot=K logs=L`（N/M/K/L 为真实删除行数，非 -1；库含 >2 天历史老数据，N 应为大数）✅；其余 #2~#8 维持通过。

## 第十四轮：第十三轮 #1 假绿修复最终验收通过——8/8 全真 PASS，大屏 8 项问题整体闭环（2026-07-09 续，用户"已重启"）
- 用户重跑 start-all.ps1 部署（含第十三轮 @Transactional 修复）。我**前台直接运行** `verify_dashboard.py --purge --traffic --wait 120`（遵守主人长期记忆强制：主动监控、阻塞到拿到 8 项结果再汇报，不依赖后台被动通知），EXIT=0。
- **#1 假绿消除确认**：`spans=17120 / metrics_agg=185916 / snapshot=6644 / logs=4390` 为**真实删除行数**（非 -1），证明 `@Transactional` 修复使 bulk delete 真正执行；库内含 >2 天历史老数据，删除计数大数合理。✅
- **8/8 全真 PASS**（无假绿）：
  - #2 首Token时延 P50=334 P95=514 P99=514 (ms) —— 单位修复正确（llm.* 毫秒、http.* 秒×1000）✅
  - #3 P95系统时延 p95Latency=1656.2561 → 前端显示 1656ms ✅（非旧 532000 误放大）
  - #4 错误率 分数=0.0 → 显示 0.00%（2位小数）；基于 H2 spans 真实计算，恒 0~1 分数 ✅
  - #5 业务转化率 conversionRate=None → 前端"暂无" ✅
  - #6 访问用户量 dau=6（历史统计人数 DAU）✅
  - #7 实时在线 realTimeOnline=6（流量后非0）✅
  - #8 趋势 buckets=12 请求合计=387 Token合计=235318（图例"请求量/Token"有数据）✅
- **结论**：大屏 8 项人工验证问题（清2天老数据 / 首Token=0ms / P95=0ms / 错误率2位小数 / 转化率"暂无" / 访问量=0 / 实时在线=0 / 趋势图例空）全部真值化闭环，第十二~十三轮潜伏的 532000ms 误放大 + errorRate 366% + purge 假绿三处根因均已根除并运行时验证通过。
- 本轮无代码改动，纯运行时验收闭环。大屏 8 项修复工作流正式收口。

## 第十五轮诊断：L1(1114) > L0(1019) 反向，是否清 H2 导致？
- **结论：不是清 H2 导致的。** 三项实证：
  1. L0/L1/L2 来自 **Redis 计数器** `obs:metrics:agent_call:L0/L1/L2`（非 H2）；purge 只清 H2 表（spans/metrics_agg/redis_metrics_snapshot/logs），**完全不碰 Redis**。`RedisH2SyncService` 30s 快照写的是 `redis_metrics_snapshot` 表，也不删 agent_call。
  2. 实时 `redis-cli get`：L0=1035 / L1=1132 / L2=759，TTL≈21570s（6h），**今天 purge+重启后这些计数器毫发无损保留**（用户看到的 1019/1114 是稍早值，差 ~16 为后续流量累加，一致）→ 证明 purge 未改它们。
  3. `MetricsQueryService:153-164` 优先读 Redis，仅当 Redis==0 才用 H2 spans 回退（`setAgentCallCount`，SET 非 INCR）。
- **真正根因 = 与 errorCount 366% 同源的「按 OTLP 导出次数 +1」bug**：
  - `OtlpParserService:147/150` 在**每个 `agent.*`/`llm.*` 指标 data point 摄入时**调 `incrementAgentRedis`；内部对命中的层名 `incrAgentCall(+1)`（每次 export 都 +1，非每次真实调用）。累积计数器按 scrape 周期反复导出 → 计数器被放大 N 倍。
  - L1 的匹配名集（5 个：`intent.classif`/`agent.l1`/`l1.call`/`intent.l1`/`router.l1`）比 L0（2 个：`router.decision`/`intent.recognized`）**更宽**，且每请求发射的 L1 类指标更多 → L1 累加更快，最终反超 L0。这是计数口径失真，非真实"L1 调用比 L0 多"。
- **次生 bug（H2 回退）**：`MetricsQueryService:115-122` 回退用 `findDistinctOperationNames()` 数的是**去重后的 operationName 字符串种类数**（如 "L0:qwen3-8b" 算 1 个），不是真实 span 条数；且 `setAgentCallCount` 用 SET 覆盖（TTL 仅 120s），语义错。若 Redis TTL 过期又恰好 H2 被 purge，会用一个极小"种类数"覆盖累计值。
- **推荐修复（待主人确认是否实施）**：弃用 per-export `incrAgentCall`，改为基于 H2 spans **真实计数**（窗口内 `operation_name LIKE 'L0:%' / 'L1:%' / 'L2:%'`，`L1:` 前缀天然排除 L1-LLM1/L1-LLM2 子调用），按 60s 节流缓存进 Redis（同 errorRate 模式）。这样 L0≥L1 符合拓扑、且不受导出频率干扰。

## 第十六轮：实施 L0/L1/L2 基于 H2 span 真实计数（修复 L1>L0 反向）
- 主人确认"好的"→ 实施第十五轮推荐修复。改动：
  1. `SpanRepository` 新增 `countByOperationNamePrefixSince(prefix, from)`（原生 SQL `operation_name LIKE :prefix AND start_time >= :from`）。
  2. `MetricsQueryService`：新增 `getAgentCallCountsFromSpans()`（2d 窗口、`L0:%`/`L1:%`/`L2:%` 前缀、60s 节流缓存 cachedL0/L1/L2），在 `getRealtime()` 方法作用域（if(needFallback) 块之外）计算并 `setAgentCallCount` 进 Redis；VO 的 L0/L1/L2 与 rerouteRate 分母改用这些真实值。删除旧 `findDistinctOperationNames()`「数去重 opName 种类数」次生 bug。
  3. `OtlpParserService`：删除 `incrementAgentRedis` 方法 + 指标摄入循环里两处调用（根除 per-export +1 失真源头）。`incrAgentCall`(RedisMetricsService) 现无人调用，保留无害。
- 踩坑：初次把 `countL0/L1/L2` 声明放在 `if (needFallback) { }` 块内 → 方法作用域外不可见，编译报"找不到符号"；移到块外（方法作用域）后 `mvnw -o compile` BUILD SUCCESS。
- 文档：`docs/specs/指标GAP分析-v2.md` 补 8.13 节（L1>L0 根因 + 修复 + 清 H2 非成因澄清）。
- 状态：代码改完并编译通过，待主人 `start-all.ps1` 重编部署；部署后 L0/L1/L2 显示 H2 真实调用数（L0 ≥ L1），不再随导出频率漂移。可选：在 `verify_dashboard.py` 加断言 `L0 >= L1` 防回归。

## 第十七轮（22:50 用户重启后验证 L0/L1/L2）
- **前台拉取实时数据（遵守长期记忆：主动轮询不放后台）**：`/realtime` 返回 `l0Calls=183 / l1Calls=62 / l2Calls=204`（Redis 同步 183/62/204）。
- **核心修复生效**：`L1=62 < L0=183`，第十五轮诊断的"L1>L0 反向"已根除；且证实**与清 H2 无关**（现基于 H2 spans 真实计数 `operation_name LIKE 'L0:%'/'L1:%'/'L2:%'`，窗口 2d）。
- **新现象 L2=204 > L0=183**：基于 `TraceQueryService.java:473-477,604-606` 的层级模型——`L0:`=入口路由(每请求1个)，`L1:`=意图Agent，`L2:`=工作流层(一个工作流展开多个 `L2:xxx` 步骤 span)。故"按 span 条数"计数时 L2 自然可能 > L0，属正常语义，非 Redis 失真（已与第十五轮 per-export bug 区分）。
- **运行 H2 被 backend 独占 MVStore 锁**：`cp`=Device busy、`FILE_LOCK=NO`/VSS `vssadmin` 均无法只读直查运行库 → 无法 100% 确认 L2 是否多 span。已向用户提议加只读诊断端点 `GET /api/v1/ai/debug/span-layers`（下次重启时拉取）彻底确认，并顺带把 L1 计数补 `L1-` 前缀（`TraceQueryService:476` 显示 L1 层含 `L1-` 形式，当前 `LIKE 'L1:%'` 漏掉；但盲目补可能让 L1 反超 L0 暴露真实拓扑，故先查 H2 再定）。
- **第十六轮重启复验（前台拉取，23:04）**：`/realtime` 仍 `l0Calls=183 / l1Calls=62 / l2Calls=204`，与第十五轮完全一致 → 证实 H2 真实计数是**确定性值**，不再随 Redis 导出频率漂移（per-export 失真已根除）。L1<L0 修复稳固。诊断端点 + L1 精确化尚未写代码（属上轮待用户确认项），本次重启跑的是既有 jar。
- **第十七轮：彻底清空 H2+Redis 待 seed 验证（23:08）**：用户要求清 H2 和 Redis 所有数据 → 我重跑 seed 验证。操作：① Redis `FLUSHALL`（DBSIZE→0）✅；② 仅 kill 后端 java PID 49288（9090，不动 Core PID 42392/8080）→ 端口释放 → 删除 H2 文件 `observability/backend/data/observability.mv.db`(484MB活动库)+`.bak.mv.db`+根 `data/observability.mv.db`(1.6MB旧残留) 全部删除✅。现在数据层全空，Core 仍在跑。待用户 `start-all.ps1` 重启 → H2 由 schema.sql 重建空库 → 我跑 `python3 test/seed_all.py`（36 轮对话，覆盖 seed.sh+seed_new.sh，中文 UTF-8 安全）灌数据 → 再验 L0/L1/L2 与趋势。
- **第十八轮：干净基线 seed 验证（23:13，前台跑+主动等结果）**：重启后 H2 由 schema.sql 重建空库(266KB)；seed_all.py 36 轮全部送达 Core(SEED_EXIT=0)。验证全绿：errorRate=0.0(分数→显示0.00%)；ttftP95=589ms；p95Latency=1907ms；dau=12；realTimeOnline=12；/trend 12桶、末桶 19请求/45068 Token（45s 时看到0桶是因30分钟桶未闭合，非bug）。**确认原"L1>L0(1114>1019)"= per-export Redis 失真，已根除**：干净基线 `L0=25 / L1=3 / L2=54`，L1<L0。
- **第十八轮续：L1 漏算根因（代码确认，非猜测）**：Core `AgentSpanContext` 层命名含 `L0:`(DomainRouter)、`L1:`(ChatService，仅CHAT域)、`L1-LLM1:`(ContextRouter)、`L1-LLM2:`(SubGraphRouter)、`L2:`(各Graph)。`MetricsQueryService.getAgentCallCountsFromSpans` 的 L1 查询 `LIKE 'L1:%'` 只命中 `L1:`(仅CHAT域3条)，漏掉 `L1-LLM1/L1-LLM2`（其余域意图识别走这两个子span）→ L1=3 被低估。`L2=54 > L0=25` 是"工作流展开多 step span"的真实 span 条数语义（非bug）。
- **第十八轮待决策（语义问题，非纯bug，需用户拍板）**：L0/L1/L2 究竟按「原始 span 条数」还是「每 trace 调用次数(去重)」统计？前者因含 L1-LLM*/L2-step 子 span，L1/L2 会 > L0（真实但反直觉）；后者 L0≥L1≥L2 直观（每请求每层计1次）。根因已清，等用户选口径再改。
- **第十八轮续2（用户决策+实现）**：用户明确「L1 虽有 2 个 LLM 子调用，但 agent 视角仍是 1 个，agent 统计=1」。据此实现：① `SpanRepository` 新增 `countDistinctTraceByOpNamePrefixSince(prefix, from)`（`COUNT(DISTINCT trace_id)`）；② `MetricsQueryService.getAgentCallCountsFromSpans` 改用该方法，L0=`L0:%`/L1=`L1%`(覆盖 L1:/L1-LLM1:/L1-LLM2:)/L2=`L2:%`。结果天然 L0≥L1≥L2（正常流每请求各1次；仅工作流请求有 L2，故 L2≤L1=L0）。`./mvnw.cmd -o compile` BUILD SUCCESS。待用户 `start-all.ps1` 重启 → 我重跑 seed_all.py + 验证 L0/L1/L2 应呈 L0≥L1≥L2 且各≈去重 trace 数。

## 第十九轮：第十八轮 trace 去重口径最终验收（用户"已重启"）
- 用户重跑 start-all.ps1 部署（含第十八轮 `countDistinctTraceByOpNamePrefixSince` 改动）。我**前台主动轮询 seed_all.py 直到 36/36 完成**（遵守长期记忆：不依赖后台被动通知，持续 TaskOutput 直到拿到 EXIT=0），SEED_EXIT=0，全部对话送达 Core（含 TRANSFER/BILL/WEALTH/CHAT/DISAMBIGUATION/无法识别/INTERRUPTED 多分支）。
- 等待落库后拉 `/realtime`：初次 `l0=20/l1=20/l2=20`（requestCount=32，还在增长）。**排除 60s 缓存假象**：等 65s 后重算仍 `l0=20/l1=20/l2=20`，此时 requestCount=36（36 条全部落库）→ 确认是 H2 真实值，非缓存冻结。
- **后端 trace 去重口径验证通过** ✓：SQL `COUNT(DISTINCT trace_id)` + 前缀 `L0:%`/`L1%`/`L2:%` + 2d 窗口 + 60s 节流，三者相等满足 `L0≥L1≥L2`（符合用户"每请求计1次"决策）。命名体系经 `debug/span-stats` 的 `distinctOpNames` 确认正确：`L0:qwen3.6-35b-a3b`/`L1:...`/`L1-LLM1:...`/`L1-LLM2:...`/`L2:...` 均存在且能被前缀匹配。
- **遗留根因（Core 端，非后端查询 bug）**：`L0=20 < requestCount=36`。`distinctOpNames` 实测含退化的 `llm:qwen3.6-35b-a3b`——即 `ObsChatModel.startBusinessSpan` 在 `AgentSpanContext.get()==null` 时 spanName 退化为 `llm:<model>`（`ObsChatModel.java:406-408`），该 span 不被 `L0:` 前缀匹配 → 不计入 L0/L1/L2。代码定位：Core 全仓库仅 `DomainRouter.java:97` 一处 `set("L0",...)`，部分请求路径（规则/缓存/特定分支或 ThreadLocal 跨线程丢失）未经过该 set 即调用 LLM → 产生 `llm:` 退化 span。共 16 个请求的 LLM 调用未带 agent.layer。
- **结论**：后端「按 trace 去重」口径已正确实现并运行时验证闭环；数值 20(<36) 是 Core 埋点不完整（AgentSpanContext 未全路径传递）导致，非后端 SQL/命名/缓存问题。
- **建议修复（待用户拍板）**：Core 端确保 DomainRouter/意图Agent/工作流的**每次 LLM 调用前都正确 set AgentSpanContext**（或用 OTel Context/Baggage 跨线程传播 ThreadLocal），使所有业务 span 带 `agent.layer` → 退化 `llm:` 消失 → L0 可达 requestCount。无需改后端查询。
- 本轮无后端代码改动，纯运行时验收。
