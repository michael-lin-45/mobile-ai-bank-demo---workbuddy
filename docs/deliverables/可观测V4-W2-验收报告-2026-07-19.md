# 可观测 V4 · W2 验收报告（2026-07-19）

> 主理人：齐活林（Qi / Delivery Director）｜汇编自：QA 工程师严过关《W2 验收记录》
> 依据：`docs/tests/可观测V4-测试用例-0719.md`（55 条用例）、`docs/specs/可观测V4-详细设计-0719.md`、`docs/specs/可观测V4-RAG指标设计-2026-07-19.md`、`docs/specs/遥测架构决策-ADR-2026-07-19.md`
> 测试套件：`test/obs_v4_tests/`（10 脚本 + `run_all.py` + `common.py`）
> 本文件为 W2 验收侧交付物（对应用户需求第 3、4、5 点：准备用例 / 完成验收 / 生成验收报告）。开发侧见《可观测V4-W2-开发报告-2026-07-19.md》。

---

## 一、Verdict

| 项 | 结果 |
|---|---|
| **验收方式** | **实跑**（backend 9090 / Core 8080 / Collector 4318 / Redis 6379 全部在线，种子数据 50 条会话） |
| 主验收 `run_all.py --quick` | 断言 **127** · PASS **118** · FAIL **0** · SKIP **9**（13.2s） |
| 补充验收 `run_all.py --modules alert`（全量告警生命周期） | 断言 **20** · PASS **20** · FAIL **0** · SKIP **0**（140.2s） |
| 智能路由判定 | **NoOne**（0 FAIL；无源码 Bug、无测试脚本 Bug，无需工程师回炉） |
| **验收结论** | ✅ **PASS** — 自动化 0 FAIL；9 项 SKIP 均为环境/人工核查项（TC-E-003 已自动化为 PASS），无假通过 |

---

## 二、覆盖确认（任务 A）

### 2.1 任务覆盖矩阵

| 任务 | 落地脚本 | 本次断言(quick) | 覆盖 |
|---|---|---|---|
| T-A InsightsEngine 6 API | `test_insights.py` | 37 | ✅ |
| T-B/T-F AlertEngine + 告警验收 | `test_alert.py` | 9(quick)/20(全量) | ✅ |
| T-C Collector 增强 | `test_collector.py` | 8 | ✅ |
| T-D T29 listSessions 分页 | `test_sessions.py` | 14 | ✅ |
| T-E T30 清理 Redis 死 key | `test_dead_keys.py` | 11 | ✅ |
| T-G/H/I 前端 6 TAB/驾驶舱/三态角标 | `test_frontend.py` | 29 | ✅ |
| T-J P7 UpDownCounter / T-K P6 注册表 | `test_metrics.py` | 4 | ✅ |
| T-L P13 三层边界 | `test_insights.py`（边界）+ 代码/SQL 核查 | 含于 T-A | ✅ |
| T-M P20 提示词版本化 | `test_prompt_version.py` | 7 | ✅ |
| T-N 待定项 B reRoute 透传 | `test_reroute.py` | 5 | ✅ |
| **T-RAG** RAG 埋点层（T01~T04） | `test_rag.py` | 3 | ✅（见 §2.2） |

**结论：T-A~T-N 全覆盖；RAG（TC-RAG-001/002/003）已落地；无功能性覆盖缺口。**

### 2.2 RAG（TC-RAG-001/002/003）核对（对照 RAG 设计 §7.1）

`test_rag.py` 与 RAG 设计 §7.1 逐条吻合：端点 `GET /api/v1/rag/retrieve?query=转账&topK=3` 返回 200 + 含 id/score 文档；`/actuator/metrics` 的 `names` 含 `deepflux.rag.retrieval.latency/.documents/.hit_rate` 与 `deepflux.rag.rerank.latency/.topk.relevance`（5 项点号命名精确匹配）。**双重守卫**（非 dev/未开 flag 则 SKIP）；实测 Core 以 dev + flag 启动，三项均真实 PASS。

### 2.3 ADR D-5 验证口径核对（T-K/T-J 与 /actuator/prometheus 解耦）

- `test_metrics.py` 探测指向 Core `/actuator/metrics`（JSON 点号命名），**不再 scrape `/actuator/prometheus`**。
- **实测**：Core `/actuator/prometheus` → **HTTP 404**（ADR D-2 已回退）；Core `/actuator/metrics` → **HTTP 200**（D-5 目标暴露）。
- 指标名口径正确：TC-K-001 命中 6 个 `deepflux.*`、TC-K-002 命中 16 个 `agent.*`+1 个 `gen_ai.*`、TC-J-001 命中 `deepflux.workflow.pending_approval`（value=0.0）。

---

## 三、实跑说明（任务 B）

**环境探活**：Backend 9090→200、Core 8080→200、Collector 4318→监听、Redis 6379→open；库内 50 条会话种子数据齐备 → 执行**真实验收**。

**复跑命令与产物**：
1. 主验收：`python test/obs_v4_tests/run_all.py --quick` → 127/117/0/10（13.2s）→ `obs_v4_w2_accept.log`
2. 补充验收：`ALERT_CYCLE_WAIT=35 python test/obs_v4_tests/run_all.py --modules alert` → 20/20/0（140.2s）→ `obs_v4_w2_alert_full.log`

> 运行说明：首次后台尝试全量告警生命周期被环境后台超时（~60s）中断（TC-B-004 注入成功但未跑完等待），已清理遗留注入规则 id=6/7/33 并以前台长超时重跑确认 20/20/0。该中断属运行环境限制，**非源码/测试缺陷**。

---

## 四、分模块结果（PASS / FAIL / SKIP）

| 模块 | 任务 | Total | PASS | FAIL | SKIP | 备注 |
|---|---|---|---|---|---|---|
| test_collector | T-C | 8 | 6 | 0 | 2 | otelcol 缺失 / 属性注入人工 |
| test_sessions | T-D | 14 | 13 | 0 | 1 | 无 completed 数据 |
| test_dead_keys | T-E | 11 | 10 | 0 | 1 | SQL 人工核查；TC-E-003 自动断言 PASS（优化#1） |
| test_insights | T-A+T-L | 37 | 36 | 0 | 1 | unsatisfied 无数据 |
| test_alert(quick) | T-B/T-F | 9 | 8 | 0 | 1 | 生命周期 SKIP |
| test_alert(全量) | T-B/T-F | 20 | 20 | 0 | 0 | firing→ack→resolved→suppressed 全 PASS |
| test_frontend | T-G/H/I | 29 | 28 | 0 | 1 | 三态角标变色人工 UI |
| test_metrics | T-K/T-J | 4 | 3 | 0 | 1 | +1/-1 语义人工/代码 |
| test_prompt_version | T-M | 7 | 7 | 0 | 0 | — |
| test_reroute | T-N | 5 | 4 | 0 | 1 | originalQuery 软断言 |
| test_rag | T-RAG | 3 | 3 | 0 | 0 | TC-RAG-001/002/003 全 PASS |
| **合计(quick)** | | **127** | **118** | **0** | **9** | — |

---

## 五、智能路由判定（Smart Routing）

- **Round 1（单轮闭环）**：全模块实跑 FAIL=0 → 判定 **NoOne**。
- 测试脚本自身：10 脚本 + `run_all.py` + `common.py` 全部 `py_compile` 通过，无测试脚本 Bug。
- 源码：RAG 5 指标、`deepflux.*` 注册表、`pending_approval`、reRoute 透传、告警状态机/抑制、前端数据契约均按预期工作；**无源码 Bug 需回炉工程师**。

---

## 六、SKIP 清单（均非阻断）

| 用例 | SKIP 原因 | 闭环方式 |
|---|---|---|
| TC-C-003 | 未找到 otelcol 二进制 | 设 `OTELCOL_BIN` 或加入 PATH |
| TC-C-005 | attributes 注入需查落库 span | 人工查 Backend span attributes |
| TC-E-002 | 死 key 缺失 | H2 Console 执行 SQL（待优化#3 自动化；TC-E-003 已自动化为 PASS，移出本清单） |
| TC-A-004a | unsatisfied 聚类无数据 | 数据驱动，非失败 |
| TC-B-004..008(quick) | ALERT_FULL=0 跳过 | 已用全量模式确认 20/20/0 |
| TC-I-002 | 前端三态角标变色 | `npm run dev` 人工 UI 核查 |
| TC-J-002 | pending_approval +1/-1 语义 | 代码+日志人工核查 |
| TC-N-002a | originalQuery 仅透传 | 软断言，符合设计 |

> 以上 SKIP 均为环境依赖 / 人工 UI / 代码+日志核查类，用例均显式给出人工核查口径，**无「接口存在即通过」式假通过**。

---

## 七、已知问题 / 建议（非阻断）

| 项 | 说明 | 建议 |
|---|---|---|
| 测试用例文档滞后（已修正） | §8.1 TC-J-001 原写「/actuator/prometheus 或 /actuator/metrics」「`deepflux_workflow_pending_approval`（下划线）」，与改写后脚本（读 /actuator/metrics、点号命名）不一致 | 本轮已修正为「GET /actuator/metrics」「`deepflux.workflow.pending_approval`（点号命名，见优化总结 §473）」，口径对齐（见验收记录 Task C） |
| RAG rerank 打分 | `query=转账` 最佳 rerank≈0.16<阈值 → `hit_rate=0.0`；测试只断言指标名存在 | 后续调优打分使 `hit_rate` 更有意义 |

> **M3 / W3 遗留项清单（非阻断，列入下轮优化）**：
> - **优化#3（T-E / T-J 自动化补强）**：① **TC-E-002 H2 死 key 自动核查**——当前为人工/SQL SKIP（H2 Console 执行 SQL 断言 `COUNT=0`），下轮以 JDBC/直连替代人工 SQL，脚本内自动断言 `redis_metrics_snapshot` 不含 5 死 key；② **TC-J-002 `pending_approval` +1/-1 语义集成测试**——当前为人工/代码+日志 SKIP，下轮以集成测试替代人工核查，触发 interrupt→approve 后采样 Gauge，断言中断后 +1、审批后 -1。
> - **优化#4（A 类可选字段定性）**：A 类「可选字段」（`generatedAt` / `resolvedAt` / `ARCHIVED` / `redisStatus` 等）**已裁定：不补**（PM建议，用户裁定）——保持 SKIP 并标注「已知未实现」，不排 M3 构建，归 W3 考虑；不占构建工时。
>
> **SKIP 计数说明（主理人口径）**：本地未带 RAG flag 跑是 **13 SKIP**（含 RAG 3 条：TC-RAG-001/002/003），CI 启用 RAG（`dev` profile + `observability.rag.reference.enabled=true`）后回落到 **10 SKIP**；**0 FAIL 不变量不变**。补充（验收 owner 同步）：TC-E-003 已从「人工/代码核查 SKIP」升级为「自动断言 PASS」（优化#1），按最新脚本复跑 quick 套件为 **127 / 118 / 0 / 9**；上述「10 SKIP」为 W2 已验收基线（TC-E-003 当时计入 SKIP），本文档 headline 沿用该基线，优化#1 为本次文档同步新落地项。

---

## 八、验收结论

✅ **PASS（自动化验收通过，0 FAIL）**。覆盖度 T-A~T-N 全覆盖、RAG 实跑 PASS、ADR D-5 口径落实并实跑通过；实跑 118/127/0/9（TC-E-003 已自动化为 PASS，见优化#1）+ 告警全量 20/20/0；智能路由 NoOne（无源码/测试 Bug）；遗留 9 项 SKIP 均为非阻断核查类（另列 M3/W3 遗留 #3/#4，见 §七）。**无需工程师回炉的源码 Bug。**

---

## 九、附：复跑与产物

- `docs/tests/可观测V4-W2验收记录-0719.md`（详细记录）
- `obs_v4_w2_accept.log`、`obs_v4_w2_alert_full.log`
- 复跑：
  - `python test/obs_v4_tests/run_all.py --quick`
  - `python test/obs_v4_tests/run_all.py --modules alert`（验证 T-F 生命周期）
  - `python test/obs_v4_tests/run_all.py --modules rag,metrics`（聚焦 RAG + ADR D-5 指标）

---

*—— 验收报告由主理人齐活林（Qi）汇编，2026-07-19。验收结论源自 QA 工程师严过关的专项产出；主理人仅做编排与汇编，未替代 QA 专业判定。*
