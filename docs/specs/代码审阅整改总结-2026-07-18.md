# AI 可观测功能代码审阅整改总结（2026-07-18）

## 背景
基于两份来源逐条交叉验证代码：
1. 外部审阅《代码改进点汇总.md》—— 22 条（P0×6 / P1×11 / P2×5）
2. V6-web 文档《可观测优化总结-0711-WB V6-web文章合入.md》—— 22 条 web 合入观点（🔴6 / 🟡16）

本轮定位：对**代码 bug 类**（外部审阅 P0/P1/P2）动手修复；对**架构演进类**（V6-web 观点）给出 roadmap 结论，不盲目大改 DEMO 项目。

---

## 一、已修复（14 项，全部编译/校验通过）

| # | 来源 | 问题 | 修复方案 | 验证 |
|---|------|------|----------|------|
| T22 | P0-5 | 前端 `/trace` 跳转不带 traceId，点了看不到对应 trace | `SessionViewerPage`/`LogViewerPage` 的 `handleTraceClick` 改为 `/trace?traceId=...`；`TraceExplorerPage` 挂载时读 URL query 预填筛选并定位该 trace | 人工 review（eslint 不可用） |
| T23 | P2 | `ObsChatModel.java:41` 注释称"不创建含业务属性 OTel Span"，与已实现矛盾，误导维护者 | 改为准确描述（L0/L1 span 已注入 `session_id`/`intent`/`model`/`routing.mode`） | Core compile OK |
| T24 | V6-P2 | Collector 零 processor 纯透传，浪费能力 | `config.yaml` 加 `memory_limiter` + `batch` processor，pipelines 引用 | `otelcol validate` OK |
| T25 | P0-6 | 秒级指标靠硬编码 2 项白名单，未来漏加→1000× 失真 | `isSecondsUnitMetric` 改为命名规则判断：`http.*.duration` 且非 `llm.*`（未来新增 http 类时长指标自动命中） | Backend compile OK |
| T26 | P0-3 | `SessionBridge` 手工 `String.format` 拼 JSON，Unicode 控制字符转义不全 | 改用 Jackson `ObjectMapper` 序列化（`escape` 仅作 fallback） | Core compile OK |
| T27 | P0-1 | `BankController.reportSession` 硬编码 `confidence=0.0`，会话回放页永远显示 0 | 新增 `currentConfidenceRef` 传 `domainResult.confidence()` 真实置信度（tokens 仍 0：规则模式无 LLM 调用，待接 token 计量） | Core compile OK |
| T28 | P0-2 | `SessionService.toTurnVO` 硬编码 `ttftMs=0L` / `model="qwen-plus"` | 用已注入 `spanRepository` 按 `traceId` 查 spans 解析真实 model（取 `L0:qwen-*` 的 operationName）+ TTFT（首 LLM span 起始 − 首 span 起始，毫秒） | Backend compile OK |
| T31 | T25 二次加固 | T25 仅覆盖 `http.*.duration`，**非 http 的秒级自定义指标**（如 `db.query.duration`/`rpc.*.duration`/`agent.*.duration`）仍漏 ×1000，错一个数量级 | `isSecondsUnitMetric` 改为通用命名规则：`name.endsWith(".duration") || name.endsWith(".latency")`，并排除 `llm.*`（已知毫秒级）。任一时长类后缀指标自动命中，不再依赖 `http.` 前缀白名单 | Backend touch 后全量重编 `Compiling 75 source files` BUILD SUCCESS |
| T32 | T28 二次加固 | T28 的 model 解析硬编码 `op.contains("qwen")`，若真实模型为 `L0:gpt-4o`/`L0:claude-3`/`L0:deepseek` 会被**误标**为"规则模式(确定性路由)" | `toTurnVO` 改为凡 `L0:` 前缀的 span 即取 `op.substring(3)` 为真实模型名（兼容任意供应商），无 `L0:` span 才回退"规则模式(确定性路由)" | Backend compile OK（同 T31 重编） |
| T33 | 体验整改（新诉求） | `seed.sh`/`seed_new.sh` 只能看任务下发、看不到对错响应，跑错也不知道 | 重写 `send()` 自动探测 `python3`/`python`；成功打印 `✅ [STATUS] INTENT — 答案前60字`、失败打印 `❌ HTTP {code} — …`/`❌ ERR — …`；`/tmp/seed_counter_$$.txt` 计 ok/fail，结尾 `>>> 播种结果：N 成功 / M 失败`，有失败则 `exit 1`（供 CI/自动化感知） | `bash -n` 两脚本通过；端口 8080 探活 UP |
| T34 | 体验整改（新诉求，"整个项目都要定义时区"） | 会话回放时间时区不对（原 `Instant.toString()` 是 UTC，差 8h），且项目未统一时区 | 四层兜底：① `SessionService.java` 新增 `BEIJING=Asia/Shanghai`+`BJ_FMT`，`toTurnVO` 的 time 与 `formatTimeRange` 改用 BJ_FMT；② `application.yml` `spring.jackson.time-zone: Asia/Shanghai`；③ `application.yml` `hibernate.jdbc.time_zone: Asia/Shanghai`；④ `scripts/start-all.ps1` 的 `$OBS_JVM_MEM`/`$CORE_JVM_MEM` 均加 `-Duser.timezone=Asia/Shanghai` | Backend `./mvnw compile`（touch 后）`Compiling 75 source files` BUILD SUCCESS |
| T35 | 体验整改（新诉求） | 链路追踪（TraceTable）无"时间"列，`TraceListVO` 已有 `Instant timestamp` 但前端未渲染 | 新建 `frontend/src/utils/time.js` 导出 `formatBeijingTime(value)`（`Intl.DateTimeFormat('zh-CN',{timeZone:'Asia/Shanghai',hour12:false})`，兼容 ISO串/毫秒/秒时间戳）；`TraceTable.jsx` 在 Trace 列后增 `<th>时间</th>` + `<td>{formatBeijingTime(t.timestamp)}</td>` | Frontend `npm run build` `✓ built in 46.79s` |
| T36 | 体验整改（新诉求，业务验证发现） | Core `POST /api/bank/chat` 非 SSE 模式直接 500：`HttpMessageNotWritableException: No converter for [class WorkflowOutput] with Content-Type 'null'`；种子脚本跑不出全绿、失败归因困难 | `BankController.chat` 返回类型 `Object`→`ResponseEntity<?>`；删除 `@PostMapping` 的 `produces`（避免 content negotiation 选中 `text/event-stream` 而缺转换器）；SSE 分支改 `ResponseEntity.ok().contentType(TEXT_EVENT_STREAM).body(sseAdapter.toSse(...))`；空消息分支 `return ResponseEntity.ok(error.toWorkflowOutput())`；补 `import org.springframework.http.ResponseEntity` | `./mvnw package -DskipTests` BUILD SUCCESS（先 `taskkill` 停占用 jar 的 Core 进程释放文件锁再 package）；新 jar 重启后 `/api/bank/chat` 非 SSE 模式返回 200 + 合法 `WorkflowOutput` JSON，空/正常请求均 200，不再 500 |
| T37 | 体验整改（新诉求，"整个项目都要定义时区"缺口） | Core 的全局错误响应 `timestamp` 仍是 `+00:00`（UTC），项目时区未真正统一（仅后端 9090 配了 `jackson.time-zone`，Core 8080 漏配） | Core `src/main/resources/application.yml` 的 `spring:` 下补 `jackson: time-zone: Asia/Shanghai`，与后端 9090 一致 | 重启 Core 后全局错误响应 `timestamp=2026-07-18T23:43:45.758+08:00`（原 `+00:00`）；启动日志时间戳亦为 `+08:00` |

---

## 二、待修复（2 项，确认属生产演进 / 低风险）

| # | 来源 | 问题 | 现状 | 计划 |
|---|------|------|------|------|
| T29 | P1 | `SessionService.listSessions` 接口有 `page/size` 但实现全量拉时间范围会话到内存再切片，会话量大时 OOM/延迟 | DEMO 量级（几十~几百会话）可接受 | 生产环境改 DB 分页（`PageRequest` + `Specification` 或 `findAll` 返回 `Page`） |
| T30 | P1 | Redis 死 key：`accuracy:intent` 的 `setIntentAccuracy` 无调用方；`RedisH2SyncService` 同步的 gauge 恒为 null | 主指标已绕过——`MetricsQueryService:166` 历史 P0-3 修复已改从 H2 实时计算 `intentAccuracy`，Redis key 死但不影响主指标 | 生产清理时删除该 dead gauge 同步逻辑 |

---

## 三、反馈误判 / 已过时（无需改）

- **`listSessions` 分页接口参数已存在**（L52-55 有 `page/size`，返回含 `totalElements/totalPages`），仅实现未真分页（见 T29）。
- **`accuracy:intent` 主准确率指标已在历史 P0-3 修复中改 H2 实时计算**，Redis key 虽死但不影响主指标（见 T30）。
- **`SessionBridge` 手工 JSON 的 `escape()` 已覆盖 `"` `\` `\n` `\r` 基础场景**，反馈对风险略高估；已用 Jackson 进一步加固，双保险。

---

## 四、本轮未采纳的 V6-web 演进项（生产路线图，非 bug）

方向正确但属生产架构演进，DEMO 阶段按路线图推进，不在本轮动手：

- **P1 Kafka 缓冲层** — 生产削峰填谷必需，DEMO 直连足够
- **P2 batch processor** — ✅ 本轮已做（零成本）
- **P3 Exemplar 显式化** — 需 OTel SDK 侧配合
- **P9 Collector DaemonSet** — K8s 部署实践
- **P10 PII 护栏** — ⚠️ 银行合规硬需求，**上线前必做**
- **P11 SLO 从业务向下拆 + Tier 分级** — 设计重写级
- **P12 金融落地蓝图「时序折叠」** — 合规归档规划
- **P16 动态基线替代静态阈值** — 告警降噪
- 其余 P4/P5/P6/P7/P8/P13-P15/P17-P22 为设计补强，记入路线图

---

## 五、验证命令

```bash
# Core 模块（T22/T23/T26/T27）
./mvnw.cmd compile -DskipTests -o          # → EXIT=0

# Backend 模块（T25/T28）
cd observability/backend && ./mvnw.cmd compile -DskipTests -o   # → EXIT=0

# Collector 配置（T24）
cd observability/otel-collector && ./otelcol.exe validate --config config.yaml   # → EXIT=0

# 前端构建（T35）
cd observability/frontend && npm run build                                       # → ✓ built in ~47s

# 播种脚本语法检查（T33）
bash -n test/seed.sh && bash -n test/seed_new.sh                                  # → 无输出即语法 OK

# Core 重新打包 + 重启验证（T36/T37）
./mvnw package -DskipTests               # → BUILD SUCCESS（需先停占用 jar 的 Core 进程释放文件锁再 package）
# 重启 Core 后业务验证：非 SSE 模式应返回 200 + 合法 WorkflowOutput JSON（不再 500）
python3 -c "import urllib.request,json; r=urllib.request.urlopen(urllib.request.Request('http://127.0.0.1:8080/api/bank/chat?sessionId=v',data=json.dumps({'message':'查一下我的账户余额'}).encode(),headers={'Content-Type':'application/json'}),timeout=45); print(r.status, r.read().decode()[:200])"
```
