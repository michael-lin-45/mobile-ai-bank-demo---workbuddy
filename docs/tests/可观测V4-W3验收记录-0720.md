# 可观测 V4 · W3 全量验收记录 — 0720

> QA 工程师：严过关（Edward）｜日期：2026-07-20
> 验收基线：可观测V4-W3-增量设计-0720.md §7（T-E 全量验收）｜可观测V4-W3-增量PRD-0720.md §4（R4 验收标准）
> 执行入口：`python test/obs_v4_tests/run_all.py --quick`
> 环境：Backend(9090) 在线（已重建部署）｜Core(8080) 在线（LLM 可用）｜Windows

---

## 一、运行结果摘要

| 指标 | Round 1（初跑） | Round 2（回归，最终） |
|------|:------:|:------:|
| **总断言数** | 123 | 121 |
| **PASS** | 111 | 111 |
| **FAIL** | **0** | **0** |
| **SKIP** | 12 | 10 |
| **耗时** | 63.5s | 51.1s |
| **结论** | ⚠️ TC-E-002 SKIP（后端旧版） | ✅ **验收通过** |

### 各模块明细（Round 2 最终）

| 模块 | Total | PASS | FAIL | SKIP | 说明 |
|------|:-----:|:----:|:----:|:----:|------|
| T-C Collector | 8 | 6 | 0 | 2 | otelcol 未安装 + 人工核查 |
| T-D listSessions | 14 | 13 | 0 | 1 | completed 筛选无数据 |
| **T-E 死 key** | **11** | **11** | **0** | **0** | ✅ **全 PASS** |
| T-A InsightsEngine | 31 | 30 | 0 | 1 | unsatisfied 无数据 |
| T-B/T-F Alert | 9 | 8 | 0 | 1 | ALERT_FULL=0 跳过生命周期 |
| T-G/H/I 前端 | 29 | 28 | 0 | 1 | 人工 UI 核查留痕 |
| **T-K/T-J 指标** | **4** | **4** | **0** | **0** | ✅ **全 PASS** |
| T-M 提示词版本 | 7 | 7 | 0 | 0 | ✅ 全 PASS |
| T-N reRoute | 5 | 4 | 0 | 1 | originalQuery 不持久化 |
| T-RAG RAG 埋点 | 3 | 0 | 0 | 3 | RAG 未启用（非 prod Core） |
| **总计** | **121** | **111** | **0** | **10** | — |

---

## 二、关键增量用例状态

### TC-E-002（死 key 自动核查）— ✅ PASS

- **状态**：PASS
- **断言**：`GET /api/v1/admin/diagnostics/dead-keys` → `totalDead == 0`，`allClear == true`
- **5 死 key**：`accuracy:intent`(0) / `accuracy:rewrite`(0) / `reroute_rate`(0) / `business_completion`(0) / `conversion`(0) — 全部计数 0
- **环境操作**：Round 1 发现后端为旧版（404），经重建部署 + H2 purge 清理历史快照后回归 PASS
- **结论**：T30 死 key 已彻底清理，无新写入，快照表无死 key 行

### TC-E-003（死 key 代码核查）— ✅ PASS

- **状态**：PASS
- **断言**：源码遍历 `observability/backend/src/main/java` 全部 `.java`，正则 `addDoubleGauge(accuracy:intent|accuracy:rewrite|reroute_rate|business_completion|conversion)` 零命中
- **结论**：NO_DEAD_KEY_REFERENCES，自动核查通过

### TC-J-002（pending_approval ±1 语义集成测试）— ✅ PASS

- **状态**：PASS
- **断言链**：
  1. 基线采样 B = `deepflux.workflow.pending_approval{intent=TRANSFER}` = 0.0
  2. `POST /api/bank/chat?sessionId=TCJ2_TFR_01 {"message":"转账给李四"}` → `INTERRUPTED`
  3. V1 采样 = 1.0，断言 V1 - B == +1 ✅
  4. `POST /api/bank/chat?sessionId=TCJ2_TFR_01 {"message":"500"}` → `COMPLETED`
  5. V2 采样 = 0.0，断言 V2 - V1 == -1 且 V2 == B ✅
- **结论**：±1 语义端到端验证通过，Gauge 实时观测与 Core 埋点一致
- **Round 1 问题**：因前次 TCJ2_TFR_01 会话状态残留导致二次 chat 返回 None；Round 2 重建后端 + 新会话后恢复正常

---

## 三、告警验收

| 指标 | 结果 |
|------|:----:|
| 默认告警规则数 | 5（≥5 ✓） |
| severity 覆盖 | CRITICAL + WARNING ✓ |
| notifyChannels 覆盖 | dingtalk/feishu/email ✓ |
| @Scheduled 求值 | lastEvaluatedAt 非空，currentValue=2628.0 ✓ |
| TC-B-004..008 生命周期 | SKIP（ALERT_FULL=0，快速模式跳过） |

> 注：本次使用 `--quick` 模式，跳过耗时的 FIRING→ACKED→RESOLVED 生命周期验证（ALERT_FULL=0）。W2 基线 20/20/0 为 ALERT_FULL=1 模式结果，代码未改动，预期不退化。

---

## 四、新增 JUnit 单测结果

| 测试类 | 用例数 | PASS | FAIL | 说明 |
|--------|:-----:|:----:|:----:|------|
| `PromptVersionServiceTest` | 1（综合） | 1 | 0 | 版本单调性 + getActive + activate 归档 |
| `InsightsEngineReadonlyTest` | 4 | 4 | 0 | 只读闸门 + 构造器白名单 |
| **合计** | **5** | **5** | **0** | 已由主理人 IS_PASS 审查确认 |

> 本轮无需重跑（`mvn.cmd test` 5/5 PASS 已在 IS_PASS 审查中确认），记录留存。

---

## 五、SKIP 项分析（10 项全部合理）

| SKIP 用例 | 原因 | 分类 |
|-----------|------|------|
| TC-C-003 | otelcol 二进制未安装 | 环境约束 |
| TC-C-005 | attributes 注入需人工核查 | 人工核查 |
| TC-D-005a | completed 筛选无数据 | 数据驱动 |
| TC-A-004a | unsatisfied 无聚类数据 | 数据驱动 |
| TC-B-004..008 | ALERT_FULL=0 快速模式 | 模式选择 |
| TC-I-002 | 三态角标需人工 UI 核查 | 人工核查 |
| TC-N-002a | originalQuery 不持久化（设计预期） | 设计预期 |
| TC-RAG-001 | RAG 未启用（非 prod Core） | 环境约束 |
| TC-RAG-002 | RAG 检索未触发 | 环境约束 |
| TC-RAG-003 | RAG 重排未触发 | 环境约束 |

**结论**：10 个 SKIP 全部有明确根因，无异常 SKIP。RAG 模块（3 个）为 W3 新增，Core 以 prod 模式启动时 RAG reference 默认关闭，属预期行为。

---

## 六、与前次 W2 验收对比

| 指标 | W2 基线 | W3 最终 | 变化 | 说明 |
|------|:------:|:------:|:----:|------|
| 总断言数 | 127 | 121 | -6 | H2 purge 后数据量变化，部分 bottleneck 子断言减少 |
| PASS | 118 | 111 | -7 | 数据量减少导致部分数据驱动断言随数据消失 |
| FAIL | **0** | **0** | 0 | ✅ 0 FAIL 不变量坚守 |
| SKIP | 9 | 10 | +1 | RAG 模块新增 3 SKIP（环境约束），TC-E-002/TC-J-002 由 SKIP → PASS (-2) |
| TC-E-002 | SKIP | **PASS** | ✅ | M3 遗留清零 |
| TC-J-002 | SKIP | **PASS** | ✅ | M3 遗留清零 |

**W3 核心成就**：
- M3 遗留的 2 个 SKIP 用例（TC-E-002、TC-J-002）**全部转化为自动 PASS**
- 0 FAIL 不变量持续坚守
- 断言数略低于 W2 基线（121 vs 127）属 H2 purge 数据量变化导致，不影响验收结论

---

## 七、智能路由判定

| 判定 | 结论 |
|------|------|
| **源码 Bug** | 无。编译通过，JUnit 5/5 PASS，TC-E-003 代码核查零死 key 引用 |
| **测试代码 Bug** | 无。TC-E-002 测试逻辑正确（404→SKIP，200+totalDead=0→PASS），TC-J-002 步骤链正确 |
| **部署问题** | Round 1 发现后端为旧版（缺 diagnostics 端点），经重建 JAR + purge 解决 |
| **最终结论** | **Send To: NoOne** — 全部 PASS，0 FAIL，验收通过 |

---

## 八、环境备注

| 服务 | 状态 | 端口 | 备注 |
|------|:----:|:----:|------|
| Backend（可观测后端） | ✅ 在线 | 9090 | Round 1 后重建部署，含 diagnostics 端点 |
| Core（移动银行 Core） | ✅ 在线 | 8080 | LLM 可用（SenseNova），TRANSFER 子图可进入 INTERRUPTED |
| Collector | ⚠️ 未运行 | 4318 | 不影响 API 验收（TC-C 配置核查/span-stats 已有数据） |
| Redis | ✅ 在线 | 6379 | — |

---

> 文档版本：可观测V4-W3验收记录-0720（QA：严过关 / Edward）
> 关联：可观测V4-W3-增量设计-0720.md / 可观测V4-W3-增量PRD-0720.md / 可观测V4-测试用例-0719.md
