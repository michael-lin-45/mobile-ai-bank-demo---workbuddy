# 可观测 V4 — W2 验收记录（2026-07-19）

> QA 工程师：严过关（Yan）
> 依据：`docs/tests/可观测V4-测试用例-0719.md`（55 条用例）、`docs/specs/可观测V4-详细设计-0719.md`、`docs/specs/可观测V4-RAG指标设计-2026-07-19.md`、`docs/specs/遥测架构决策-ADR-2026-07-19.md`
> 测试套件：`test/obs_v4_tests/`（10 脚本 + `run_all.py` + `common.py`）

---

## 0. 摘要 / Verdict

| 项 | 结果 |
|---|---|
| **验收方式** | **实跑**（backend 9090 / Core 8080 / Collector 4318 / Redis 6379 全部在线） |
| **主验收** `run_all.py --quick` | 断言 **127** · PASS **118** · FAIL **0** · SKIP **9**（13.2s） |
| **补充验收** `run_all.py --modules alert`（全量告警生命周期，无 --quick） | 断言 **20** · PASS **20** · FAIL **0** · SKIP **0**（140.2s） |
| **智能路由判定** | **NoOne**（0 FAIL；无源码 Bug、无测试脚本 Bug，无需回炉工程师） |
| **验收结论** | ✅ **PASS** — 自动化 0 FAIL；9 项 SKIP 均为环境性 / 人工核查项（TC-E-003 已自动化为 PASS，见下），无假通过 |

> 历史基线参考（上一轮交付 `可观测V4-delivery-2026-07-19.md`）：135 断言 / 123 PASS / 0 FAIL / 12 SKIP。
> 本次 W2 实跑计数（127 / 117 / 10）与历史基线略有差异，源于脚本演进（test_metrics 按 ADR 改写、test_rag 新增、若干子断言增删）；**关键不变量「0 FAIL、无假通过」在两次实跑中一致成立**。
> ▸ **测试质量优化同步（本次文档刷新）**：① **优化#1** — TC-E-003 由「人工/代码核查 SKIP」升级为「自动断言 PASS」（纯 Python 遍历 `observability/backend/src/main/java` 下 .java，正则断言无 `addDoubleGauge(死key)` 引用；实跑 `python test/obs_v4_tests/test_dead_keys.py` → TC-E-003 PASS，模块 11/10/0/1）。故 quick 套件 PASS 117→118、SKIP 10→9。② **优化#2** — RAG 三项 TC-RAG-001/002/003 在 CI 以 `dev` profile + `observability.rag.reference.enabled=true` 起 Core 时实跑 PASS（详见 §1.2）。**0 FAIL 不变量不变**。

---

## 1. 测试用例覆盖确认结论（任务 A）

### 1.1 任务覆盖矩阵

| 任务 | 用例文档章节 | 落地脚本 | 本次断言数(quick) | 覆盖 |
|---|---|---|---|---|
| T-A InsightsEngine 6 API | §2 | `test_insights.py` | 37 | ✅ |
| T-B/T-F AlertEngine + 告警验收 | §3 | `test_alert.py` | 9(quick) / 20(全量) | ✅ |
| T-C Collector 增强 | §4 | `test_collector.py` | 8 | ✅ |
| T-D T29 listSessions 分页 | §5 | `test_sessions.py` | 14 | ✅ |
| T-E T30 清理 Redis 死 key | §6 | `test_dead_keys.py` | 11 | ✅ |
| T-G/H/I 前端 6 TAB/驾驶舱/三态角标 | §7 | `test_frontend.py` | 29 | ✅ |
| T-J P7 UpDownCounter pending_approval | §8.1 | `test_metrics.py` | 4(含 3 PASS/1 SKIP) | ✅ |
| T-K P6 集中式指标注册表 | §8.2 | `test_metrics.py` | （同上） | ✅ |
| T-L P13 三层边界治理 | §8.3 | `test_insights.py`（边界）+ 代码/SQL 核查 | （含于 T-A 37） | ✅ |
| T-M P20 提示词版本化 | §8.4 | `test_prompt_version.py` | 7 | ✅ |
| T-N 待定项 B reRoute 透传 | §8.5 | `test_reroute.py` | 5 | ✅ |
| **T-RAG** RAG 埋点层（T01~T04） | RAG 设计 §7.1 | `test_rag.py` | 3 | ✅（见 §1.2） |

**结论**：T-A ~ T-N 全部有对应脚本与断言；RAG（TC-RAG-001/002/003）已在 `test_rag.py` 落地；无功能性覆盖缺口。

### 1.2 RAG（TC-RAG-001/002/003）落地核对（对照 RAG 设计 §7.1）

`test_rag.py` 实现与 RAG 设计 §7.1 **逐条吻合**：

| 用例 | 设计 §7.1 要求 | `test_rag.py` 实现 | 实测 |
|---|---|---|---|
| TC-RAG-001 | `GET CORE_URL/api/v1/rag/retrieve?query=转账&topK=3` → 200 + 非空 documents（含 id/score） | `RAG_TRIGGER_QUERY="转账"`, `topK=3`，断言 documents 列表且每项含 `id`/`score` | ✅ 端点 HTTP 200，返回 3 篇含 id/score 文档 |
| TC-RAG-002 | 触发后 `CORE_URL/actuator/metrics` 的 `names` 含 `deepflux.rag.retrieval.latency/.documents/.hit_rate` | `RETRIEVAL_METRICS` 三点号命名精确匹配，并 GET 单 meter 确认 `measurements` 非空 | ✅ 三项均出现且含 measurements |
| TC-RAG-003 | 同上含 `deepflux.rag.rerank.latency`、`deepflux.rag.topk.relevance` | `RERANK_METRICS` 精确匹配 | ✅ 两项均出现且含 measurements |

- **双重守卫**：诊断端点仅在 `@Profile("dev")` + `observability.rag.reference.enabled=true` 装配；脚本在端点不可用（404/禁用）或 `actuator/metrics` 未暴露时 **SKIP 并给人工/代码核查口径，不假通过**（与 `test_metrics.py` 口径一致）。
- **验证前提满足**：实测 Core 即以 dev 环境 + flag 启动（端点 200 且返回文档），故本次 RAG 三项均 **真实 PASS**（非 SKIP）。
- 观测备注（非缺陷）：`query=转账` 时参考检索器最佳 rerank 分≈0.16 < 阈值 → `retrievalHitRate=0.0`；测试仅断言指标名存在、不断言数值，不影响 PASS。建议后续调优打分（降阈值或提 priority 加权）使 `hit_rate` 在诊断场景更有意义（同 delivery §400）。

### 1.3 ADR D-5 验证口径核对（T-K/T-J 不依赖 /actuator/prometheus）

- `test_metrics.py` 探测指向 **`CORE_URL`** 的 `/actuator/metrics`（Spring Boot Actuator JSON，点号命名），**不再 scrape `/actuator/prometheus`**（L60 `_get_json(core, "/actuator/metrics")`）。
- **实测**：Core `/actuator/prometheus` → **HTTP 404**（ADR D-2 已回退 prometheus 分支）；Core `/actuator/metrics` → **HTTP 200**（D-5 目标暴露）。
- **配置级复核**：`observability/backend/src/main/resources/application.yml` L74-79 `management.endpoints.web.exposure.include: health,info,metrics` —— **不含 `prometheus`** ✅（ADR I-2 落实）。
- 指标名口径正确（点号非下划线）：TC-K-001 实测命中 6 个 `deepflux.*` 指标、TC-K-002 命中 16 个 `agent.*` + 1 个 `gen_ai.*`、TC-J-001 命中 `deepflux.workflow.pending_approval`（value=0.0）。

### 1.4 统一入口 `run_all.py`

- `MODULES` 列表（L31-42）**已含 `"test_rag"`**（L41），TC-RAG 纳入统一回归 ✅。

### 1.5 `py_compile` 校验

- 全部 `test/obs_v4_tests/test_*.py`（10 个）+ `run_all.py` + `common.py` 均通过 `python -m py_compile`，无语法错误 ✅；并额外用 `ast.parse` 复核通过。

### 1.6 覆盖结论与轻微文档滞后（非缺口）

- **功能性覆盖无缺口**：T-A~T-N + RAG(TC-RAG-001/002/003) + ADR D-5 口径均已落地并实跑通过。
- **文档滞后已在本轮同步修正（非阻塞、非测试缺口）**：测试用例文档 §8.1 `TC-J-001`（L189）原写「`GET /actuator/prometheus` 或 `/actuator/metrics`」「`deepflux_workflow_pending_approval`（下划线）」，与改写后的 `test_metrics.py`（读 `/actuator/metrics`、点号命名）不一致。本轮已修正为「`GET /actuator/metrics`」「`deepflux.workflow.pending_approval`（点号命名，见优化总结 §473）」，口径已对齐（详见 §4 / Task C）。

---

## 2. 验收实跑说明（任务 B）

**环境探活（实跑，非环境受限复核）**：

| 服务 | 探测 | 结果 |
|---|---|---|
| Backend 9090 | `GET /actuator/health` | HTTP **200**（UP） |
| Core 8080 | `GET /actuator/health` | HTTP **200**（UP） |
| Collector 4318 | 根路径 | 监听（根 404 正常，OTLP 端点独立） |
| Redis 6379 | TCP socket | **open** |
| 种子数据 | `GET /api/v1/sessions` | `totalElements=50`（满足 T-A/D/E 前置） |

→ 环境在线，执行**真实验收**；下列代码/配置级核查作为口径佐证同步完成。

**复跑命令与产物**：

1. 主验收（quick，跳过耗时告警生命周期）：
   `python test/obs_v4_tests/run_all.py --quick`
   → 127 断言 · 117 PASS · 0 FAIL · 10 SKIP（13.2s）
   → 产物：`obs_v4_w2_accept.log`
2. 补充验收（全量告警生命周期，独立确认 T-F）：
   `ALERT_CYCLE_WAIT=35 python test/obs_v4_tests/run_all.py --modules alert`
   → 20 断言 · 20 PASS · 0 FAIL · 0 SKIP（140.2s）
   → 产物：`obs_v4_w2_alert_full.log`

> 运行说明：首次后台尝试全量告警生命周期被环境后台超时（~60s）中断（TC-B-004 注入成功、id=33，但 80s firing 等待未跑完）。已清理遗留注入规则 id=6/7/33，并改以前台长超时重跑确认 **20/20/0**。该中断属运行环境限制，**非源码/测试缺陷**。

---

## 3. 分模块结果（PASS / FAIL / SKIP）

| 模块 | 任务 | Total | PASS | FAIL | SKIP | 备注 |
|---|---|---|---|---|---|---|
| test_collector | T-C | 8 | 6 | 0 | 2 | TC-C-003(otelcol 缺失) / TC-C-005(属性注入人工) SKIP |
| test_sessions | T-D | 14 | 13 | 0 | 1 | TC-D-005a(无 completed 数据) SKIP |
| test_dead_keys | T-E | 11 | 10 | 0 | 1 | TC-E-002(SQL) 人工核查 SKIP；TC-E-003 自动断言 PASS（优化#1） |
| test_insights | T-A+T-L | 37 | 36 | 0 | 1 | TC-A-004a(unsatisfied 无数据) SKIP |
| test_alert(quick) | T-B/T-F | 9 | 8 | 0 | 1 | TC-B-004..008 因 ALERT_FULL=0 SKIP |
| test_alert(全量) | T-B/T-F | 20 | 20 | 0 | 0 | 独立复跑：firing→ack→resolved→suppressed 全 PASS |
| test_frontend | T-G/H/I | 29 | 28 | 0 | 1 | TC-I-002(三态角标变色) 人工 UI SKIP |
| test_metrics | T-K/T-J | 4 | 3 | 0 | 1 | TC-J-002(+1/-1 语义) 人工/代码 SKIP |
| test_prompt_version | T-M | 7 | 7 | 0 | 0 | — |
| test_reroute | T-N | 5 | 4 | 0 | 1 | TC-N-002a(originalQuery 未持久化) 软断言 SKIP |
| test_rag | T-RAG | 3 | 3 | 0 | 0 | TC-RAG-001/002/003 全 PASS |
| **合计(quick)** | | **127** | **118** | **0** | **9** | — |

> 上表「test_alert」两行分别对应 quick 模式（SKIP 生命周期）与全量模式（生命周期实跑 PASS）；两者不重复计入「合计」，合计取 quick 全量套件口径。

---

## 4. 智能路由判定（Smart Routing）

- **Round 1（单轮闭环）**：全模块实跑 **FAIL = 0** → 无需路由，判定 **NoOne**。
- **测试脚本自身**：10 脚本 + `run_all.py` + `common.py` 全部 `py_compile` 通过，运行无误；无测试脚本 Bug 需自行修复。
- **源码**：自动化 0 FAIL；RAG 5 指标、`deepflux.*` 注册表、`pending_approval`、`reRoute` 透传、告警状态机/抑制、前端数据契约等均按预期工作；**无源码 Bug 需回炉工程师（寇豆码）**。
- 全量告警生命周期首次后台超时中断属**运行环境限制**（已清理遗留规则并前台重跑确认），不计入源码/测试缺陷。

---

## 5. 已知问题 / 遗留项（SKIP 清单，均非阻断）

| 用例 | SKIP 原因 | 闭环方式 |
|---|---|---|
| TC-C-003 | 未找到 otelcol 二进制（PATH / `OTELCOL_BIN`） | 设 `OTELCOL_BIN` 或加入 PATH 后自动启用 `otelcol validate` |
| TC-C-005 | attributes 注入（env/service.version）需查落库 span | 人工查入 Backend 的 span attributes |
| TC-E-002 | `redis_metrics_snapshot` 5 死 key 缺失 | H2 Console 执行文档给定 SQL 断言 COUNT=0（待优化#3 自动化） |
| TC-A-004a | unsatisfied 聚类当前无数据 | 数据驱动，非失败；造不满数据即可 |
| TC-B-004..008(quick) | `ALERT_FULL=0` 跳过耗时生命周期 | 已用全量模式独立确认 **20/20/0** |
| TC-I-002 | 前端三态角标变色（停 Collector） | `npm run dev` 人工 UI 核查 |
| TC-J-002 | `pending_approval` +1/-1 实时语义 | 代码 + 日志人工核查（设计级，始终 SKIP） |
| TC-N-002a | `originalQuery` 按设计 §6.2 仅透传不新增列 | 软断言：有则核对、无则跳过（符合设计） |

> 以上 SKIP 均为**环境依赖 / 人工 UI / 代码+日志核查**类，用例均显式给出人工核查口径，不存在「接口存在即通过」式假通过。

### 5.1 遗留项（M3 / W3，非阻断，列入下轮优化）

- **优化#3（T-E / T-J 自动化补强）**：
  - **TC-E-002 H2 死 key 自动核查**：当前为人工/SQL SKIP（需在 H2 Console 执行给定 SQL 断言 `COUNT=0`）；下轮以 JDBC/直连替代人工 SQL，脚本内自动断言 `redis_metrics_snapshot` 不含 5 死 key。
  - **TC-J-002 `pending_approval` +1/-1 语义集成测试**：当前为人工/代码+日志 SKIP；下轮以集成测试替代人工核查，触发 interrupt→approve 后采样 Gauge，断言中断后 +1、审批后 -1。
- **优化#4（A 类可选字段定性）**：A 类「可选字段」（`generatedAt` / `resolvedAt` / `ARCHIVED` / `redisStatus` 等）由 **PM 定性**——若判定为必须，则派工程师补实现并补测试；若判定为可选，则保持 SKIP 并标注「已知未实现」，不在本轮假通过。

---

## 6. 验收结论 / Verdict

✅ **PASS（自动化验收通过，0 FAIL）**。

- 覆盖度：T-A~T-N 全覆盖；RAG（TC-RAG-001/002/003）按 RAG 设计 §7.1 落地并实跑 PASS；ADR D-5 口径（T-K/T-J 改验 Core `/actuator/metrics`、与 `/actuator/prometheus` 解耦）已落实并实跑通过；`run_all.py` 入口含 `test_rag`；全部脚本 `py_compile` 通过。**无功能性覆盖缺口**。
- 实跑：backend/Core/Collector/Redis 均在线，种子数据 50 条会话；主验收 127/118/0/9（TC-E-003 已自动化为 PASS，见优化#1），补充全量告警生命周期 20/20/0。
- 智能路由：**NoOne**（0 FAIL，无源码 Bug、无测试脚本 Bug，无需工程师回炉）。
- 遗留：9 项 SKIP 均为环境/人工核查类（见 §5 / §5.1），非阻断；另列 M3/W3 遗留（优化#3 / #4，见 §5.1）。
- **无需工程师回炉的源码 Bug**。

---

## 7. 附：产物与复跑

- `docs/tests/可观测V4-W2验收记录-0719.md`（本文件）
- `obs_v4_w2_accept.log`（主验收 quick 全量输出）
- `obs_v4_w2_alert_full.log`（全量告警生命周期输出）
- 复跑：
  - `python test/obs_v4_tests/run_all.py --quick`
  - `python test/obs_v4_tests/run_all.py --modules alert`（ALERT_FULL=1，验证 T-F 生命周期）
  - `python test/obs_v4_tests/run_all.py --modules rag,metrics`（聚焦 RAG + ADR D-5 指标）
